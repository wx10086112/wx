# 零点科技团购商城 - 更新日志

> 按日期记录所有前后端修改内容

---

## 2026-05-25 新增分销商层级与超级管理员视角切换设计 ✅ 已完成

### 业务背景
- 当前系统是平台直接管理多个商家。
- 后续会出现不同区域的分销商。
- 每个分销商下面可以管理多个商家。
- 超级管理员是最高权限，可以管理所有分销商、所有商家和全平台数据。
- 分销商只能管理自己名下的商家，不能查看平级分销商或其他分销商下的商家。
- 商家只能查看和管理自己的经营数据，不能查看平级商家。

### 新权限层级

```text
超级管理员 SUPER_ADMIN
  └── 分销商 DISTRIBUTOR_ADMIN
        └── 商家 MERCHANT_ADMIN / MERCHANT_STAFF
```

权限边界：

| 角色 | 数据范围 | 能否切换身份 |
|------|------|------|
| 超级管理员 | 全平台所有分销商、商家、订单、团购、结算 | 可以切换为任意分销商视角 |
| 分销商 | 仅自己名下商家及其业务数据 | 不可以切换 |
| 商家 | 仅自己商家的业务数据 | 不可以切换 |

### 数据关系

```text
platform_admin 超级管理员
  └── distributor 分销商
        └── merchant 商家
              ├── merchant_store 门店
              ├── product / groupon_activity / groupon_activity_item 商品与团购
              ├── mall_order / order_item 订单
              ├── write_off_record 核销
              └── merchant_settlement_record 结算
```

### 数据库建议

新增分销商表：

```text
distributor
```

建议字段：

| 字段 | 说明 |
|------|------|
| `id` | 分销商 ID |
| `name` | 分销商名称 |
| `contact` | 联系人 |
| `phone` | 联系电话 |
| `username` | 登录账号 |
| `password` | 登录密码，BCrypt |
| `region_code` | 区域编码，可选 |
| `region_name` | 区域名称，可选 |
| `status` | 0 禁用，1 正常 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |
| `remark` | 备注 |

商家表增加：

```text
merchant.distributor_id
```

建议核心业务表冗余 `distributor_id`：

| 表 | 建议 |
|------|------|
| `merchant` | 必加 `distributor_id` |
| `mall_order` | 建议加 `distributor_id` |
| `order_item` | 建议加 `distributor_id` |
| `groupon_activity` | 建议加 `distributor_id` |
| `groupon_activity_item` | 建议加 `distributor_id` |
| `merchant_settlement_record` | 建议加 `distributor_id` |
| `write_off_record` | 建议加 `distributor_id` |

说明：

- 商家归属分销商是强关系。
- 订单、结算、核销等高频查询表冗余 `distributor_id`，便于分销商后台快速过滤。
- 即使冗余了 `distributor_id`，后端保存时也必须从 `merchant.distributor_id` 推导，不允许前端随意传入覆盖。

### 后端数据权限规则

所有后台接口必须按当前登录身份过滤数据：

| 登录身份 | 查询条件 |
|------|------|
| `SUPER_ADMIN` | 默认不过滤，可按 `distributorId` / `merchantId` 查询 |
| `SUPER_ADMIN` 切换为分销商 | 强制 `distributor_id = viewDistributorId` |
| `DISTRIBUTOR_ADMIN` | 强制 `distributor_id = 当前分销商 ID` |
| `MERCHANT_ADMIN` / `MERCHANT_STAFF` | 强制 `merchant_id = 当前商家 ID` |

后端不能只靠前端隐藏菜单，必须在 Service 或统一数据权限层做强校验。

### 超级管理员切换分销商视角

超级管理员可以进入任意分销商视角：

```text
超级管理员后台
└── 分销商管理
    └── 切换为该分销商
```

切换后 token/session 建议保存：

```json
{
  "realRole": "SUPER_ADMIN",
  "viewRole": "DISTRIBUTOR_ADMIN",
  "viewDistributorId": 12
}
```

分销商自己登录时：

```json
{
  "realRole": "DISTRIBUTOR_ADMIN",
  "viewRole": "DISTRIBUTOR_ADMIN",
  "distributorId": 12
}
```

约束：

- 只有 `SUPER_ADMIN` 能切换。
- `DISTRIBUTOR_ADMIN` 不能切换。
- 切换视角后，后台页面看到的数据应与该分销商自己登录一致。
- 超级管理员需要有“退出分销商视角 / 返回超级管理员”入口。

### 后台菜单建议

超级管理员后台：

```text
分销商管理
├── 分销商列表
├── 新增分销商
├── 重置密码
├── 启用/禁用
└── 切换为分销商

商家管理
├── 全部商家
└── 商家详情

订单管理
财务结算
数据分析
系统管理
```

分销商后台：

```text
商家管理
├── 我的商家
└── 商家详情

订单管理
财务结算
数据分析
```

### 后端接口建议

新增总后台分销商管理接口：

```text
GET    /mall/distributor/list
GET    /mall/distributor/{id}
POST   /mall/distributor
PUT    /mall/distributor
DELETE /mall/distributor/{ids}
PUT    /mall/distributor/status
PUT    /mall/distributor/reset-password
POST   /mall/distributor/switch/{id}
POST   /mall/distributor/switch-back
```

接口用途：

| 接口 | 说明 |
|------|------|
| `/mall/distributor/list` | 分销商列表 |
| `/mall/distributor/{id}` | 分销商详情 |
| `POST /mall/distributor` | 超级管理员创建分销商 |
| `PUT /mall/distributor` | 修改分销商资料 |
| `DELETE /mall/distributor/{ids}` | 删除/逻辑删除分销商 |
| `PUT /mall/distributor/status` | 启用/禁用分销商 |
| `PUT /mall/distributor/reset-password` | 重置分销商密码 |
| `POST /mall/distributor/switch/{id}` | 超级管理员切换到分销商视角 |
| `POST /mall/distributor/switch-back` | 返回超级管理员视角 |

### 前端 API 写入位置

建议新增：

```text
ruoyi-ui/src/api/distributor/index.js
```

方法建议：

```js
listDistributor(query)
getDistributor(id)
addDistributor(data)
updateDistributor(data)
deleteDistributor(ids)
changeDistributorStatus(data)
resetDistributorPassword(data)
switchDistributor(id)
switchBackDistributor()
```

### 前端页面写入位置

建议新增：

```text
ruoyi-ui/src/views/distributor/list.vue
ruoyi-ui/src/views/distributor/detail.vue
```

第一版也可以只做：

```text
ruoyi-ui/src/views/distributor/list.vue
```

分销商列表页面功能：

| 功能 | 说明 |
|------|------|
| 搜索 | 按名称、手机号、区域、状态 |
| 新增 | 超级管理员创建分销商 |
| 编辑 | 修改资料 |
| 重置密码 | 超级管理员重置分销商密码 |
| 启用/禁用 | 控制分销商能否登录 |
| 查看商家 | 查看该分销商名下商家 |
| 切换为分销商 | 超级管理员进入该分销商视角 |

### 路由写入位置

如果继续使用静态路由，写入：

```text
ruoyi-ui/src/router/index.js
```

建议路由：

```js
{
  path: '/distributor',
  component: Layout,
  meta: { title: '分销商管理', icon: 'peoples' },
  children: [
    {
      path: 'list',
      component: () => import('@/views/distributor/list'),
      name: 'DistributorList',
      meta: { title: '分销商列表' }
    }
  ]
}
```

### 权限码建议

| 权限码 | 说明 |
|------|------|
| `mall:distributor:list` | 查询分销商 |
| `mall:distributor:query` | 查看分销商详情 |
| `mall:distributor:add` | 新增分销商 |
| `mall:distributor:edit` | 编辑分销商 |
| `mall:distributor:remove` | 删除分销商 |
| `mall:distributor:status` | 启用/禁用 |
| `mall:distributor:resetPwd` | 重置密码 |
| `mall:distributor:switch` | 切换为分销商 |

### 实施顺序建议

1. 新增 `distributor` 表。
2. `merchant` 表增加 `distributor_id`。
3. 订单、团购、结算、核销等表补 `distributor_id`。
4. 新增后端分销商 Controller / Service / Mapper。
5. 增加统一数据范围判断：超级管理员、分销商、商家。
6. 实现超级管理员切换分销商视角。
7. 新增 `ruoyi-ui/src/api/distributor/index.js`。
8. 新增 `ruoyi-ui/src/views/distributor/list.vue`。
9. 商家列表、订单列表、团购列表、结算列表接入数据范围过滤。
10. 最后补菜单权限和按钮权限。

---

## 2026-05-25 团购商品增加菜品搭配/菜品组设计 ✅ 已完成

### 业务背景
- 团购商品不只是一个简单名称和价格，还需要像餐饮套餐一样配置“菜品搭配”。
- 一个团购商品可以包含多个菜品组。
- 一个菜品组下可以添加多道菜品。
- 每道菜品可以设置名称、数量、单位、单价。
- 也可以开启“直接设置菜品总价”，不逐项计算单价。

### 页面位置

该能力属于团购商品编辑表单的一部分：

```text
商家管理
└── 商家列表
    └── 查看详情
        └── 团购活动
            └── 团购商品
                └── 新增/编辑团购商品
                    └── 菜品搭配
```

不是全局菜品库，也不是平台统一菜品。

### UI 参考结构

表单区块名称：

```text
菜品搭配
```

顶部功能：

| 控件 | 说明 |
|------|------|
| `AI 帮你搭配菜品` | 可先做按钮占位，后续再接 AI |
| `直接设置菜品总价` | 开关；开启后可以手动填写套餐菜品总价 |

菜品组结构：

```text
菜品组
├── 菜品组名称
├── 菜品 1：名称/ID + 数量 + 单位 + 单价 + 删除
├── 菜品 2：名称/ID + 数量 + 单位 + 单价 + 删除
├── 添加
├── 粘贴录入
├── 上移
├── 下移
└── 删除组
```

底部：

```text
添加菜品组
共 X 道菜品，实际可享用 Y 道
```

### 菜品组字段

建议团购商品明细中增加菜品组 JSON 字段：

```text
dish_groups
```

也可以单独建表，第一版建议先用 JSON 存储，降低实现复杂度。

`dish_groups` JSON 结构建议：

```json
[
  {
    "groupId": "G001",
    "groupName": "主菜",
    "sort": 1,
    "items": [
      {
        "dishName": "招牌水煮鱼",
        "dishId": null,
        "quantity": 1,
        "unit": "份",
        "price": 6800,
        "sort": 1
      }
    ]
  }
]
```

### 团购商品字段补充

建议 `groupon_activity_item` 增加：

| 字段 | 类型 | 说明 |
|------|------|------|
| `dish_groups` | text/json | 菜品组 JSON |
| `dish_total_price` | long | 菜品总价，单位分 |
| `direct_total_price` | int/boolean | 是否直接设置菜品总价 |
| `dish_count` | int | 菜品数量统计 |
| `available_dish_count` | int | 实际可享用菜品数量 |

### 菜品组表单交互

#### 1. 菜品组名称

输入框：

```text
填写菜品组名称，最多 10 个字符
```

校验：

- 菜品组名称必填。
- 最多 10 个字符。
- 名称中不能出现“任选N”“选N”等容易引起套餐选择歧义的字样。

#### 2. 添加菜品

每行菜品字段：

| 字段 | 组件 | 说明 |
|------|------|------|
| `dishName` | 输入框 | 填写单品名称或 ID |
| `quantity` | 数字输入 | 默认 1 |
| `unit` | 下拉框 | 份 / 个 / 杯 / 斤 / 盒 / 次 / 项 |
| `price` | 金额输入 | 单品价格，单位元展示、分存储 |
| 删除 | 图标按钮 | 删除该行菜品 |

操作：

- 添加：新增一行菜品。
- 粘贴录入：支持批量粘贴菜品文本，后续可解析为多行。
- 上移/下移：调整菜品组排序。
- 删除组：删除整个菜品组。

#### 3. 添加菜品组

按钮：

```text
添加菜品组
```

点击后新增一个空菜品组。

每个团购商品至少保留一个菜品组。

### 价格计算规则

当 `directTotalPrice = false`：

```text
菜品总价 = Σ(菜品单价 × 数量)
```

当 `directTotalPrice = true`：

```text
菜品总价 = 手动填写的 dishTotalPrice
```

团购商品表单中仍保留：

- 原价 `originalPrice`
- 团购价 `grouponPrice`
- 折扣 `discountRate`

建议：

- 菜品总价可以自动回填为原价。
- 团购价由运营单独填写。
- 折扣根据 `grouponPrice / originalPrice * 10` 自动计算。

### 示例

```json
{
  "merchantId": 1,
  "grouponId": 10,
  "name": "双人川味套餐",
  "content": "含主菜、凉菜和饮品",
  "originalPrice": 19800,
  "grouponPrice": 9900,
  "discountRate": 5.0,
  "directTotalPrice": false,
  "dishTotalPrice": 19800,
  "dishCount": 4,
  "availableDishCount": 4,
  "dishGroups": [
    {
      "groupName": "主菜",
      "sort": 1,
      "items": [
        {
          "dishName": "招牌水煮鱼",
          "quantity": 1,
          "unit": "份",
          "price": 9800
        },
        {
          "dishName": "麻婆豆腐",
          "quantity": 1,
          "unit": "份",
          "price": 3800
        }
      ]
    },
    {
      "groupName": "饮品",
      "sort": 2,
      "items": [
        {
          "dishName": "酸梅汤",
          "quantity": 2,
          "unit": "杯",
          "price": 1200
        }
      ]
    }
  ]
}
```

### 后端保存建议

团购商品保存接口：

```text
POST /mall/groupon/item
PUT  /mall/groupon/item
```

请求体中增加：

```json
{
  "dishGroups": [],
  "dishTotalPrice": 19800,
  "directTotalPrice": false,
  "dishCount": 4,
  "availableDishCount": 4
}
```

后端处理：

- 校验 `dishGroups` 至少有一个菜品组。
- 校验每个菜品组至少有一道菜。
- 校验菜品名称不能为空。
- 校验数量大于 0。
- 校验单价不能小于 0。
- 重新计算 `dishCount`。
- 如果未开启直接设置总价，后端重新计算 `dishTotalPrice`。

### C 端展示建议

团购商品详情页展示：

```text
套餐内容
主菜
- 招牌水煮鱼 ×1 份
- 麻婆豆腐 ×1 份

饮品
- 酸梅汤 ×2 杯
```

价格区展示：

```text
原价 ¥198
团购价 ¥99
5.0 折
```

### 重要约束

- 菜品搭配只属于当前团购商品。
- 菜品搭配不作为全局商品库。
- 不同团购商品可以有相同菜名，但价格、数量、内容互不影响。
- 菜品搭配数据必须跟随 `merchantId` 和 `grouponId` 隔离。
- 总后台只能在当前商家的团购活动下编辑该商家的菜品搭配。

---

## 2026-05-25 团购商品模型修正为活动内商品明细 ✅ 已完成

### 业务模型修正
- 团购不是简单把已有 `product` 商品绑定到活动。
- 团购活动下面应该可以维护多个“团购商品/套餐项”。
- 每个团购商品的名称、内容、原价、团购价、折扣、库存、图片等信息，只在当前团购活动中生效。
- 普通商品表里的商品价格不应因为某个团购活动而被直接覆盖。

### 正确数据关系

```text
merchant 商家
  └── groupon_activity 团购活动
        ├── groupon_activity_item 团购商品 A
        ├── groupon_activity_item 团购商品 B
        └── groupon_activity_item 团购商品 C
```

含义：

- 一个商家可以有多个团购活动。
- 一个团购活动可以有多个团购商品。
- 团购商品属于当前团购活动。
- 团购商品同时归属于当前商家。
- 团购商品的定价只在当前团购中有效。

### 新增建议表：groupon_activity_item

建议新增团购商品明细表：

```text
groupon_activity_item
```

建议字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | long | 团购商品 ID |
| `merchant_id` | long | 商家 ID |
| `groupon_id` | long | 所属团购活动 ID |
| `name` | string | 团购商品名称 |
| `title` | string | 展示标题，可与名称一致 |
| `content` | string | 商品内容/套餐内容 |
| `description` | string | 商品说明 |
| `cover_image` | string | 封面图 |
| `detail_images` | string | 详情图 JSON 数组 |
| `original_price` | long | 原价，单位分 |
| `groupon_price` | long | 团购价/现价，单位分 |
| `discount_rate` | decimal | 折扣，例如 7.5 表示 7.5 折 |
| `stock` | int | 团购库存 |
| `sales` | int | 团购销量 |
| `limit_per_user` | int | 每人限购，0 表示不限 |
| `valid_days` | int | 购买后有效天数 |
| `store_ids` | string | 可用门店 ID JSON 数组 |
| `status` | int | 0 下架，1 上架 |
| `sort` | int | 排序，越大越靠前 |
| `create_time` | datetime | 创建时间 |
| `update_time` | datetime | 更新时间 |

### 与 product 表关系

`product` 表仍可保留普通商品能力。

后续实现有两种方式：

| 方案 | 说明 | 建议 |
|------|------|------|
| 独立团购商品明细 | 团购商品全部存 `groupon_activity_item`，不依赖 `product` | 推荐 |
| 引用普通商品后覆写价格 | 明细表增加 `product_id`，同时保存团购价等覆写字段 | 可选 |

当前业务更适合第一种：**团购商品明细独立维护**。

原因：

- 团购里的商品名称、内容、价格可能和普通商品不同。
- 同一个普通商品可能出现在不同团购中，价格不同。
- 团购定价不应污染普通商品定价。
- C 端展示团购时直接读取团购商品明细更清晰。

### 总后台 UI 调整

团购活动仍放在：

```text
商家管理
└── 商家列表
    └── 查看详情
        └── 团购活动
```

在“团购活动”Tab 内，结构应调整为：

```text
团购活动列表
  └── 查看/编辑活动
        └── 团购商品列表
              ├── 新增团购商品
              ├── 编辑团购商品
              ├── 上传商品图片
              ├── 设置原价/团购价/折扣
              ├── 上架/下架
              └── 删除
```

### 团购商品表单字段

新增/编辑团购商品时，表单字段建议：

| 字段 | 组件 | 必填 | 说明 |
|------|------|------|------|
| `name` | 输入框 | 是 | 团购商品名称 |
| `content` | 文本域 | 是 | 套餐内容/服务内容 |
| `coverImage` | 图片上传 | 是 | 商品封面 |
| `detailImages` | 多图上传 | 否 | 商品详情图 |
| `originalPrice` | 金额输入 | 是 | 原价 |
| `grouponPrice` | 金额输入 | 是 | 团购价/现价 |
| `discountRate` | 自动计算/可手填 | 否 | 折扣 |
| `stock` | 数字输入 | 是 | 团购库存 |
| `limitPerUser` | 数字输入 | 否 | 每人限购 |
| `validDays` | 数字输入 | 是 | 有效期 |
| `storeIds` | 门店多选 | 否 | 可用门店 |
| `status` | 开关 | 是 | 上架/下架 |
| `sort` | 数字输入 | 否 | 排序 |

### 价格与折扣规则

- 金额统一按分存储。
- 前端展示元，提交前转为分。
- `originalPrice` 必须大于 0。
- `grouponPrice` 必须大于 0。
- `grouponPrice` 不应大于 `originalPrice`。
- `discountRate = grouponPrice / originalPrice * 10`。
- 折扣可由前端自动计算，后端也应重新计算或校验。

示例：

```json
{
  "originalPrice": 18800,
  "grouponPrice": 12900,
  "discountRate": 6.9
}
```

### 后端接口建议

团购活动主表接口保留：

```text
GET    /mall/groupon/list
GET    /mall/groupon/{id}
POST   /mall/groupon
PUT    /mall/groupon
DELETE /mall/groupon/{ids}
PUT    /mall/groupon/status
POST   /mall/groupon/image/upload
```

新增团购商品明细接口：

```text
GET    /mall/groupon/item/list?grouponId={grouponId}
GET    /mall/groupon/item/{id}
POST   /mall/groupon/item
PUT    /mall/groupon/item
DELETE /mall/groupon/item/{ids}
PUT    /mall/groupon/item/status
POST   /mall/groupon/item/image/upload
```

### 新增团购商品请求示例

```json
{
  "merchantId": 1,
  "grouponId": 10,
  "name": "双人肩颈理疗套餐",
  "content": "肩颈理疗 60 分钟 × 2 人，含热敷和放松护理",
  "coverImage": "/profile/merchant_images/1/groupon/10/item/cover_xxx.jpg",
  "detailImages": "[\"/profile/merchant_images/1/groupon/10/item/detail_a.jpg\"]",
  "originalPrice": 39800,
  "grouponPrice": 19900,
  "discountRate": 5.0,
  "stock": 100,
  "limitPerUser": 2,
  "validDays": 30,
  "storeIds": "[1,2]",
  "status": 1,
  "sort": 100
}
```

### 后端校验规则

- 新增团购商品时必须传 `merchantId` 和 `grouponId`。
- `groupon_activity.id = grouponId` 的活动必须存在。
- `groupon_activity.merchant_id` 必须等于请求中的 `merchantId`。
- 团购商品只能归属当前商家和当前活动。
- 价格、库存、有效期必须合法。
- 上架前必须有名称、内容、封面、原价、团购价、库存。

### 图片存储建议

团购活动图片：

```text
merchant_images/{merchantId}/groupon/{grouponId}/
```

团购商品图片：

```text
merchant_images/{merchantId}/groupon/{grouponId}/item/{itemId}/
```

如果团购商品未保存、没有 `itemId`，先上传到：

```text
merchant_images/{merchantId}/groupon/{grouponId}/item/temp/{token}/
```

### C 端展示规则

C 端进入某个商家的小程序后：

- 查询当前商家的团购活动。
- 查询活动下的团购商品明细。
- 商品详情页展示 `groupon_activity_item` 的名称、内容、原价、团购价、折扣、有效期、可用门店。
- 下单时应记录团购商品明细 ID，避免后续价格变化影响历史订单。

订单明细建议记录：

| 字段 | 说明 |
|------|------|
| `groupon_id` | 团购活动 ID |
| `groupon_item_id` | 团购商品明细 ID |
| `product_name` | 下单时商品名称快照 |
| `price` | 下单时团购价快照 |
| `original_price` | 下单时原价快照 |

### 实施记录（2026-05-25）

**后端：**

| 文件 | 说明 |
|------|------|
| `GrouponActivityItem.java` | 实体类，价格按分存储，包含折扣率自动计算 |
| `GrouponActivityItemMapper.java` | Mapper 接口 |
| `GrouponActivityItemMapper.xml` | MyBatis XML，含增删改查 + 按活动ID查询 + 按活动ID批量删除 |
| `IGrouponActivityItemService.java` | Service 接口 |
| `GrouponActivityItemServiceImpl.java` | Service 实现，insert/update 时自动计算折扣率 |
| `MallGrouponItemController.java` | Controller，6 个接口 + 图片上传 |

**接口清单：**

```text
GET    /mall/groupon/item/list          分页查询
GET    /mall/groupon/item/listByGroupon 按活动ID查询
GET    /mall/groupon/item/{id}          详情
POST   /mall/groupon/item              新增
PUT    /mall/groupon/item              修改
DELETE /mall/groupon/item/{ids}        删除
PUT    /mall/groupon/item/status       上下架
POST   /mall/groupon/item/image/upload 图片上传
```

**数据库：**

- 执行 `10_groupon_activity_item.sql` 创建 `groupon_activity_item` 表
- 已在 `ruoyi-cs` 库中执行

**前端：**

| 文件 | 变更 |
|------|------|
| `api/marketing/groupon.js` | 新增 6 个团购商品 API 函数 |
| `merchant/detail.vue` | 团购活动 Tab 改为独立商品管理，移除原有商品绑定逻辑 |

---

## 2026-05-25 团购活动入口调整为商家详情内管理 ✅ 已完成

### 调整背景
- 团购活动不是平台级统一活动，也不是独立营销中心里的全局活动。
- 团购活动属于某一个具体商家。
- 总后台只是代商家维护团购活动，因此入口应该跟随商家管理流程。

### 正确入口

团购活动入口调整为：

```text
商家管理
└── 商家列表
    └── 查看详情
        └── 团购活动
```

不建议作为全局一级入口：

```text
营销管理 / 团购活动
```

原因：

- 全局入口容易误解为平台统一团购。
- 当前业务是每个商家针对自己的小程序和自己的商品创建团购。
- 从商家详情进入，可以天然带上当前 `merchantId`，减少跨商家误操作。

### 商家详情页 UI 调整

在运营后台商家详情页中增加一个标签页：

```text
团购活动
```

建议商家详情页标签结构：

```text
基本信息
商品列表
团购活动
订单记录
流水记录
账户管理
```

### 团购活动页签能力

该页签只管理当前商家的团购活动：

| 功能 | 说明 |
|------|------|
| 团购活动列表 | 只查询当前商家的活动 |
| 新增团购活动 | 自动带入当前商家 `merchantId` |
| 编辑团购活动 | 只能编辑当前商家的活动 |
| 上架/下架活动 | 只操作当前商家的活动 |
| 上传活动素材 | 图片存到当前商家的目录 |
| 绑定商品 | 只能选择当前商家下的商品 |
| 解绑商品 | 只能解绑当前活动下的商品 |

### 数据约束

核心约束：

```text
groupon_activity.merchant_id = 当前商家 merchantId
product.merchant_id = 当前商家 merchantId
product.groupon_id = groupon_activity.id
```

新增团购活动时：

- 前端不需要让运营手动输入 `merchantId`。
- 直接从商家详情路由中获取当前商家 ID。
- 保存时自动提交 `merchantId`。

绑定商品时：

- 商品选择列表只能请求当前商家的商品。
- 接口参数必须带 `merchantId` 或由后端根据活动 ID 校验。
- 禁止把其他商家的商品绑定到当前商家的团购活动。

### 推荐接口调用

当前商家活动列表：

```text
GET /mall/groupon/list?merchantId={merchantId}
```

新增当前商家活动：

```text
POST /mall/groupon
```

请求体必须包含：

```json
{
  "merchantId": 1,
  "name": "周末双人团购套餐",
  "coverImage": "/profile/merchant_images/1/groupon/10/cover_xxx.jpg",
  "startTime": "2026-05-25 00:00:00",
  "endTime": "2026-06-30 23:59:59",
  "status": 1
}
```

查询当前商家可绑定商品：

```text
GET /mall/product/list?merchantId={merchantId}
```

绑定商品到活动：

```text
POST /mall/groupon/product/bind
```

请求体：

```json
{
  "grouponId": 10,
  "productIds": [101, 102]
}
```

后端必须再次校验：

- `grouponId` 对应活动存在。
- 活动属于当前商家。
- 所有 `productIds` 对应商品也属于同一商家。

### 图片存储

当前商家的团购活动图片存储到：

```text
E:\ruoyi\ruoyi-xm\merchant_images\{merchantId}\groupon\{grouponId}\
```

不能存到全局活动目录。

### UI 实现建议

不新增全局团购菜单，优先改造：

```text
ruoyi-ui/src/views/merchant/detail.vue
```

在该页面内部新增“团购活动”Tab。

如果后续需要复用组件，可以再拆：

```text
ruoyi-ui/src/views/merchant/components/GrouponActivityTab.vue
```

但第一版建议直接在商家详情页实现，减少路由和菜单改动。

---

## 2026-05-25 多商家双微信小程序 SaaS 模型补充

### 业务模型修正
- 本系统不是所有商家共用一个微信小程序。
- 每个商家都有两套独立微信小程序：
  - C 端小程序：给消费者访问该商家的商品和团购，用于下单、支付、查看核销码。
  - 商家端小程序：给该商家自己使用，用于上架商品、管理订单、扫码/手动核销、查看结算。
- 平台总后台统一管理所有商家，但每个商家的小程序、商品、团购、订单、核销和结算都按 `merchant_id` 隔离。

### 商家小程序配置

商家表需要维护两套小程序配置：

| 配置 | 用途 |
|------|------|
| `c_mini_app_id` | 商家 C 端小程序 AppID，消费者访问 |
| `c_mini_app_secret` | 商家 C 端小程序 Secret |
| `m_mini_app_id` | 商家端小程序 AppID，商家/店员访问 |
| `m_mini_app_secret` | 商家端小程序 Secret |
| `wx_pay_mch_id` | 该商家的微信支付商户号 |
| `wx_pay_api_key` | 该商家的微信支付 API 密钥 |

### 登录与数据隔离

- C 端小程序启动或登录时，后端根据当前小程序 `appid` 匹配 `merchant.c_mini_app_id`，确定当前访问的是哪个商家。
- 商家端小程序登录时，后端根据当前小程序 `appid` 匹配 `merchant.m_mini_app_id`，确定当前商家身份。
- 后端拿到 `merchant_id` 后，所有商品、团购、订单、核销、结算查询都必须加商家隔离条件。
- 不同商家的 C 端小程序不能互相看到对方商品。
- 不同商家的商家端小程序不能互相管理对方订单和商品。

### 团购活动影响

- 团购活动仍然是商家级活动，不是平台级活动。
- 总后台给商家创建团购时，必须选择商家。
- 团购活动绑定商品时，只能绑定该商家自己的商品。
- 用户从某个商家的 C 端小程序进入时，只看到该商家的团购活动和商品。
- 商家从自己的商家端小程序进入时，只能管理自己的团购、商品、订单和核销。

### 总后台职责

平台总后台需要支持：

- 维护每个商家的 C 端小程序配置。
- 维护每个商家的商家端小程序配置。
- 维护每个商家的微信支付配置。
- 代商家上架商品。
- 代商家创建和修改团购活动。
- 代商家上传商品图片和团购活动素材。
- 查看各商家的订单、核销、结算情况。

### 文档同步

- 已同步修正 `项目完整文档.md` 中关于 C 端和商家端 AppID 的说明。
- C 端 AppID 不再写死为单一 AppID，而是每个商家独立配置。
- 商家端 AppID 不再描述为“与 C 端同一 AppID”，而是每个商家独立的商家端 AppID。

---

## 2026-05-25 SaaS 多商家业务边界补充说明

### 核心定位
- 本项目是平台统一托管、多商家独立经营、按 `merchant_id` 隔离数据的本地生活团购 SaaS 系统。
- 平台方提供统一后台、后端、支付、订单、结算、商品、团购、核销能力。
- 商家入驻后，各自管理自己的门店、商品、团购活动、订单、员工和结算数据。
- 所有核心业务数据必须带 `merchant_id`，不得出现跨商家串数据。

### 四端职责

| 端 | 职责 |
|------|------|
| 平台总后台 | 平台运营使用，管理所有商家、审核入驻、代商家维护商品和团购、查看订单、处理财务和结算 |
| 商家端小程序 | 商家/店员使用，只能看到自己商家的订单、商品、核销、员工、门店和结算 |
| C 端小程序 | 消费者使用，浏览对应商家的商品/团购，下单、支付、到店核销 |
| 后端服务 | 提供统一 API、鉴权、数据隔离、微信支付、核销、结算和文件存储 |

### 数据隔离原则

以下业务数据必须按商家隔离：

- 商家信息：`merchant`
- 门店：`merchant_store`
- 商家员工：`merchant_user`
- 商品：`product`
- 商品图片：`product_image`
- 团购活动：`groupon_activity`
- 订单：`mall_order`
- 订单明细：`order_item`
- 核销记录：`write_off_record`
- 结算记录：`merchant_settlement_record`
- 商家图片目录：`merchant_images/{merchantId}/...`

后端所有查询、保存、修改、删除都必须校验 `merchant_id` 归属。

### 团购活动业务定义

- 团购活动是商家级活动，不是平台级全局活动。
- 不同商家可以针对自己的小程序、自己的门店、自己的商品创建不同团购。
- 平台总后台只是代商家进行团购活动添加、修改、上架、下架、素材上传和商品绑定。
- 总后台新增团购活动时，必须先明确所属商家 `merchantId`。
- 团购活动只能绑定同一商家的商品，禁止 A 商家的活动绑定 B 商家的商品。
- C 端小程序展示团购时，只展示当前商家或当前查询范围内有权限展示的团购商品。

### 团购数据关系

```text
merchant 商家
  └── groupon_activity 团购活动
        └── product 商品（通过 product.groupon_id 关联）
```

规则：

- `product.groupon_id IS NULL`：普通商品。
- `product.groupon_id IS NOT NULL`：团购商品。
- `product.merchant_id` 必须等于 `groupon_activity.merchant_id`。
- 商家是否有团购，不在商家表上做开关，而是看该商家是否存在有效团购活动和已绑定商品。

### 总后台代运营边界

总后台可以做：

- 代商家新增/编辑商品。
- 代商家上传商品图片。
- 代商家创建/编辑团购活动。
- 代商家上传团购封面、海报、详情图。
- 代商家绑定/解绑团购商品。
- 代商家上架/下架商品和活动。

总后台不能做：

- 不选择商家就创建商品或团购。
- 把一个商家的商品绑定到另一个商家的活动。
- 绕过 `merchant_id` 查询或修改其他商家数据。
- 将商家级团购误做成平台全局团购。

### 文件存储边界

商品图片：

```text
E:\ruoyi\ruoyi-xm\merchant_images\{merchantId}\product\{productId}\
```

团购活动图片：

```text
E:\ruoyi\ruoyi-xm\merchant_images\{merchantId}\groupon\{grouponId}\
```

数据库只保存可访问 URL，不保存 Windows 绝对路径。

---

## 2026-05-25 团购总后台 UI 设计待实现说明 ✅ 已完成

### 背景
- 团购总后台后端接口已经完成，下一步需要实现运营后台 `ruoyi-ui` 的页面。
- 本次只记录 UI 设计和实现要求，不直接修改前端代码。
- 目标是让平台运营可以在总后台为各个商家管理团购促销活动，包括新增、编辑、上下架、上传活动素材、绑定商品。

### 目标页面

建议复用现有运营后台结构，在 `ruoyi-ui` 中新增或改造以下页面：

```text
ruoyi-ui/src/views/marketing/activity.vue
```

该页面当前是占位页，可以改造成“团购活动管理”页面。

也可以后续按模块拆分为：

```text
ruoyi-ui/src/views/marketing/groupon/index.vue
ruoyi-ui/src/views/marketing/groupon/components/GrouponForm.vue
ruoyi-ui/src/views/marketing/groupon/components/ProductBindDialog.vue
```

短期建议先做单文件页面，减少改动范围。

### 菜单位置

建议放在运营后台侧边栏：

```text
营销管理 / 团购活动
```

如果当前没有营销管理菜单，也可以临时挂到：

```text
商家管理 / 团购活动
```

推荐路由：

```text
/marketing/activity
```

页面标题：

```text
团购活动管理
```

### 页面整体布局

页面按运营后台常规 Element UI 管理页结构设计：

1. 顶部筛选区
2. 操作按钮区
3. 活动表格
4. 分页
5. 新增/编辑弹窗
6. 绑定商品弹窗

建议不要做复杂大屏或营销落地页风格，保持后台管理系统的密集、清晰、可检索设计。

### 顶部筛选区

筛选字段建议：

| 字段 | 组件 | 说明 |
|------|------|------|
| `merchantId` | 商家选择器 / 输入框 | 按商家筛选 |
| `name` | 输入框 | 活动名称关键词 |
| `status` | 下拉框 | 全部 / 未启用 / 进行中 / 已结束 / 已下架 |
| `sourceType` | 下拉框 | 全部 / 总后台创建 / 商家端创建 |
| `dateRange` | 日期范围 | 活动开始时间或结束时间 |

按钮：

- 查询
- 重置
- 新增团购活动

### 活动表格字段

表格建议字段：

| 字段 | 说明 |
|------|------|
| 活动 ID | `id` |
| 活动封面 | `coverImage` 缩略图 |
| 活动名称 | `name` |
| 所属商家 | `merchantId`，后续可展示商家名称 |
| 活动时间 | `startTime` ~ `endTime` |
| 状态 | `status` |
| 已售数量 | `totalSold` |
| 每人限购 | `limitPerUser` |
| 排序 | `sort` |
| 创建来源 | `sourceType` |
| 创建时间 | `createTime` |
| 操作 | 编辑、上架/下架、绑定商品、删除 |

状态标签建议：

| 状态 | 展示 |
|------|------|
| `0` | 未启用 / 下架 |
| `1` | 进行中 |
| `2` | 已结束 |

### 新增/编辑活动弹窗

弹窗标题：

- 新增团购活动
- 编辑团购活动

表单字段：

| 字段 | 组件 | 必填 | 说明 |
|------|------|------|------|
| `merchantId` | 输入框 / 商家选择器 | 是 | 所属商家 |
| `name` | 输入框 | 是 | 活动名称 |
| `coverImage` | 图片上传 | 是 | 活动封面 |
| `posterImage` | 图片上传 | 否 | 活动海报 |
| `detailImages` | 多图上传 | 否 | 活动详情图 |
| `description` | 文本域 | 否 | 活动说明 |
| `startTime` | 日期时间选择器 | 是 | 开始时间 |
| `endTime` | 日期时间选择器 | 是 | 结束时间 |
| `limitPerUser` | 数字输入框 | 否 | 每人限购，0 表示不限 |
| `sort` | 数字输入框 | 否 | 排序，越大越靠前 |
| `status` | 单选/开关 | 是 | 是否启用 |

表单校验：

- `merchantId` 必填。
- `name` 必填。
- `coverImage` 必填。
- `startTime` 必填。
- `endTime` 必填。
- `startTime` 必须早于 `endTime`。
- `limitPerUser` 不能小于 0。
- `sort` 不能小于 0。

### 活动图片上传 UI

上传字段：

| 图片类型 | imageType | 数量 |
|------|------|------|
| 活动封面 | `cover` | 单图 |
| 活动海报 | `poster` | 单图 |
| 活动详情图 | `detail` | 多图 |

上传接口：

```text
POST /mall/groupon/image/upload
```

上传参数：

| 参数 | 说明 |
|------|------|
| `merchantId` | 当前活动所属商家 |
| `grouponId` | 编辑已有活动时传 |
| `tempToken` | 新增活动未保存时传 |
| `imageType` | `cover` / `poster` / `detail` |
| `file` | 图片文件 |

上传成功后使用返回的：

```json
{
  "url": "/profile/merchant_images/1/groupon/10/cover_xxxx.jpg",
  "fileName": "cover_xxxx.jpg",
  "relativePath": "1/groupon/10/cover_xxxx.jpg"
}
```

前端表单保存：

- `coverImage` 保存单个 URL。
- `posterImage` 保存单个 URL。
- `detailImages` 建议保存 JSON 字符串数组，例如：

```json
[
  "/profile/merchant_images/1/groupon/10/detail_a.jpg",
  "/profile/merchant_images/1/groupon/10/detail_b.jpg"
]
```

### 绑定商品弹窗

入口：

```text
活动表格操作列 / 绑定商品
```

弹窗功能：

- 展示当前活动已绑定商品。
- 支持选择同一商家的未绑定商品。
- 支持批量绑定。
- 支持批量解绑。

接口：

```text
GET  /mall/groupon/product/list?grouponId={grouponId}
POST /mall/groupon/product/bind
POST /mall/groupon/product/unbind
GET  /mall/product/list?merchantId={merchantId}
```

绑定请求建议：

```json
{
  "grouponId": 10,
  "productIds": [101, 102, 103]
}
```

解绑请求建议：

```json
{
  "productIds": [101, 102]
}
```

### API 封装建议

建议新增：

```text
ruoyi-ui/src/api/marketing/groupon.js
```

方法建议：

```js
listGroupon(query)
getGroupon(id)
addGroupon(data)
updateGroupon(data)
deleteGroupon(ids)
changeGrouponStatus(data)
uploadGrouponImage(data)
listGrouponProducts(grouponId)
bindGrouponProducts(data)
unbindGrouponProducts(data)
listProduct(query)
```

### 后端接口对接清单

| 方法 | 路径 | 用途 |
|------|------|------|
| `GET` | `/mall/groupon/list` | 活动列表 |
| `GET` | `/mall/groupon/{id}` | 活动详情 |
| `POST` | `/mall/groupon` | 新增活动 |
| `PUT` | `/mall/groupon` | 编辑活动 |
| `DELETE` | `/mall/groupon/{ids}` | 删除活动 |
| `PUT` | `/mall/groupon/status` | 上架/下架 |
| `GET` | `/mall/groupon/options?merchantId={merchantId}` | 活动下拉选项 |
| `POST` | `/mall/groupon/image/upload` | 活动图片上传 |
| `GET` | `/mall/groupon/product/list?grouponId={grouponId}` | 已绑定商品 |
| `POST` | `/mall/groupon/product/bind` | 绑定商品 |
| `POST` | `/mall/groupon/product/unbind` | 解绑商品 |
| `GET` | `/mall/product/list` | 商品列表 |

### 权限点建议

如果使用若依权限按钮，建议：

| 权限码 | 用途 |
|------|------|
| `mall:groupon:list` | 查询活动 |
| `mall:groupon:query` | 查看详情 |
| `mall:groupon:add` | 新增活动 |
| `mall:groupon:edit` | 编辑和上下架 |
| `mall:groupon:remove` | 删除活动 |
| `mall:groupon:upload` | 上传活动图片 |
| `mall:groupon:bind` | 绑定/解绑商品 |

### 交互细节

- 新增活动时，如果没有 `grouponId`，前端生成 `tempToken`，上传图片先放临时目录。
- 保存活动成功后，后端如已支持迁移临时目录，则迁移到正式活动目录；若暂未支持，可先保留临时 URL，后续优化。
- 上架前提醒运营检查活动时间、封面图、绑定商品。
- 删除活动前需要二次确认。
- 删除活动建议只做逻辑删除或禁用，不建议直接物理删除已产生订单的活动。
- 活动下架后，C 端团购列表不再展示该活动商品。

### 实施顺序建议

1. 新增 `api/marketing/groupon.js`。
2. 将 `views/marketing/activity.vue` 从占位页改为团购活动列表页。
3. 接入活动列表、查询、分页和状态筛选。
4. 增加新增/编辑弹窗。
5. 接入活动图片上传。
6. 接入上架/下架、删除。
7. 增加绑定商品弹窗。
8. 最后补权限按钮和异常提示。

---

## 2026-05-25 新增团购活动与总后台促销活动管理 ✅ 已完成

### 变更目标
- 总后台需要支持为各个商家创建、编辑、上架、下架团购促销活动。
- 总后台需要支持上传团购活动封面、海报、详情图等活动素材。
- 商家端和 C 端页面暂不强制改动，只预留和对接后端功能接口。
- 团购不作为商家主表开关，而作为商品的营销归属：商品绑定 `groupon_id` 即为团购商品，不绑定则为普通商品。

### 数据模型规则

| 表/字段 | 说明 |
|------|------|
| `groupon_activity` | 团购促销活动表，按商家创建和隔离 |
| `product.groupon_id` | 商品关联团购活动 ID，可为空 |
| `product.merchant_id` | 必须与关联活动的 `merchant_id` 一致 |

规则：

- `product.groupon_id IS NULL`：普通商品。
- `product.groupon_id IS NOT NULL`：团购商品。
- 商家是否有团购，不看商家表字段，看是否存在有效团购活动和已绑定的上架商品。
- 活动下架或过期后，C 端团购列表不再展示该活动下的商品。

### 总后台功能范围

| 功能 | 说明 |
|------|------|
| 团购活动列表 | 按商家、状态、时间筛选活动 |
| 新增团购活动 | 平台运营代商家创建活动 |
| 编辑团购活动 | 修改名称、时间、限购、排序、素材等 |
| 上架/下架活动 | 控制活动是否进入 C 端展示链路 |
| 上传活动素材 | 上传封面、海报、详情图 |
| 绑定活动商品 | 将同一商家的商品绑定到团购活动 |
| 解绑活动商品 | 将商品恢复为普通商品或移出活动 |

### 总后台接口

```text
GET    /mall/groupon/list
GET    /mall/groupon/{id}
POST   /mall/groupon
PUT    /mall/groupon
DELETE /mall/groupon/{ids}
PUT    /mall/groupon/status
GET    /mall/groupon/options?merchantId={merchantId}
POST   /mall/groupon/image/upload
GET    /mall/groupon/product/list?grouponId={grouponId}
POST   /mall/groupon/product/bind
POST   /mall/groupon/product/unbind
```

### 活动图片上传

上传接口：

```text
POST /mall/groupon/image/upload
Content-Type: multipart/form-data
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `merchantId` | long | 是 | 商家 ID |
| `grouponId` | long | 否 | 活动 ID |
| `imageType` | string | 是 | `cover` / `poster` / `detail` |
| `tempToken` | string | 否 | 活动未保存时使用 |
| `file` | file | 是 | 图片文件 |

返回示例：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "url": "/profile/merchant_images/1/groupon/10/cover_a1b2c3d4.jpg",
    "fileName": "cover_a1b2c3d4.jpg",
    "relativePath": "1/groupon/10/cover_a1b2c3d4.jpg"
  }
}
```

### 图片存储目录

团购活动图片统一存储到：

```text
E:\ruoyi\ruoyi-xm\merchant_images\{merchantId}\groupon\{grouponId}\
```

推荐结构：

```text
merchant_images/
└── {merchantId}/
    └── groupon/
        └── {grouponId}/
            ├── cover_{uuid}.jpg
            ├── poster_{uuid}.jpg
            └── detail_{uuid}.jpg
```

活动未保存、没有 `grouponId` 时，先上传到：

```text
merchant_images/{merchantId}/groupon/temp/{token}/
```

活动保存成功后再迁移到正式 `grouponId` 目录。

### 商家端接口预留

商家端页面暂不要求本次调整，但后端保留接口能力：

```text
GET  /wxmini/merchant-mini/groupon/activity/list
POST /wxmini/merchant-mini/groupon/activity/save
PUT  /wxmini/merchant-mini/groupon/activity/status
GET  /wxmini/merchant-mini/groupon/activity/options
```

商品保存接口 `POST /wxmini/merchant-mini/goods/save` 预留 `grouponId` 字段：

- `grouponId = null`：保存普通商品。
- `grouponId != null`：保存团购商品。
- 后端必须校验活动和商品属于同一商家。

### C 端接口规则

现有 C 端接口继续使用：

```text
GET /wxmini/groupon/list
GET /wxmini/groupon/detail/{id}
```

查询团购列表时只返回：

- 商品已上架。
- 商品 `groupon_id` 不为空。
- 活动状态可用。
- 当前时间在活动开始和结束时间内。

### 后端校验规则

- 总后台创建活动时必须传 `merchantId`。
- 商家必须存在且未禁用。
- `startTime` 必须小于 `endTime`。
- 活动上架前必须有封面图。
- 活动图片只能写入当前商家的目录。
- 绑定商品时必须校验商品和活动属于同一商家。
- 数据库存 URL，不存 Windows 绝对路径。

### 数据库扩展建议

当前 `groupon_activity` 已有基础字段，建议补充：

| 字段 | 说明 |
|------|------|
| `poster_image` | 活动海报图 |
| `detail_images` | 活动详情图 JSON 数组 |
| `sort` | 排序 |
| `audit_status` | 平台审核状态，可选 |
| `source_type` | 创建来源：`ADMIN` 总后台 / `MERCHANT` 商家端 |

---

## 2026-05-25 后台商家管理商品图片改为本地上传 ✅ 已完成

### 变更目标
- 后台管理系统的“商家管理 → 商家列表 → 商家详情 → 新增商品”中，商品图片字段不再要求手动填写图片 URL。
- 前端改为直接选择并上传图片，上传成功后由后端返回可访问的图片 URL，再写入商品表或商品图片表。
- 图片文件统一存储在项目目录：`E:\ruoyi\ruoyi-xm\merchant_images`。
- 目录结构沿用现有约定：`merchant_images/{merchantId}/product/{productId}/`。

### 建议目录结构

```text
E:\ruoyi\ruoyi-xm\merchant_images\
├── {merchantId}\
│   ├── avatar\
│   ├── store\
│   ├── banner\
│   └── product\
│       └── {productId}\
│           ├── main_{uuid}.jpg
│           ├── detail_{uuid}.jpg
│           └── sku_{uuid}.jpg
└── .gitkeep
```

### 后台 UI 改造建议

| 页面位置 | 原逻辑 | 新逻辑 |
|------|------|------|
| 商家管理 / 商家列表 / 商家详情 / 新增商品 | 输入图片 URL | 使用图片上传组件 |
| 商品主图 | 文本框填写 URL | 单图上传，成功后回填 `mainImage` |
| 商品详情图 | 文本框填写多个 URL | 多图上传，成功后保存到图片列表 |
| 图片预览 | 依赖外部 URL | 使用后端返回的本地访问 URL |

### 后端接口建议

新增后台管理端商品图片上传接口：

```text
POST /merchant/product/image/upload
Content-Type: multipart/form-data
```

请求参数建议：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `merchantId` | long | 是 | 商家 ID |
| `productId` | long | 否 | 商品 ID；新增商品未落库前可先传临时值或保存商品后再上传 |
| `imageType` | string | 是 | `main` / `detail` / `sku` |
| `file` | file | 是 | 图片文件 |

响应数据建议：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "url": "/profile/merchant_images/1/product/101/main_a1b2c3d4.jpg",
    "fileName": "main_a1b2c3d4.jpg",
    "relativePath": "1/product/101/main_a1b2c3d4.jpg"
  }
}
```

### 后端实现要点

- 上传根目录固定读取或配置为：`E:\ruoyi\ruoyi-xm\merchant_images`。
- 上传前校验文件类型，只允许 `jpg`、`jpeg`、`png`、`webp`。
- 上传前校验文件大小，建议单张不超过 5MB。
- 文件名使用 `{imageType}_{uuid8}.{ext}`，避免覆盖同名文件。
- 自动创建目录：`merchant_images/{merchantId}/product/{productId}/`。
- 新增商品场景建议先保存商品基础信息拿到 `productId`，再上传图片并更新 `main_image` 或 `product_image` 表。
- 如果必须在商品保存前上传，可先进入 `merchant_images/{merchantId}/product/temp/{token}/`，商品保存成功后再迁移到正式 `productId` 目录。
- 后端需要配置静态资源映射，让前端可通过 URL 访问 `merchant_images` 下的图片。

### 数据库保存建议

| 字段/表 | 说明 |
|------|------|
| `product.main_image` | 保存商品主图 URL |
| `product_image.image_url` | 保存详情图、SKU 图等多图 URL |
| `product_image.image_type` | `main` / `detail` / `sku` |
| `product_image.sort_order` | 多图排序 |

### 注意事项

- 数据库存 URL，不存 Windows 绝对路径。
- Windows 绝对路径只用于后端文件落盘，例如 `E:\ruoyi\ruoyi-xm\merchant_images\1\product\101\main_xxx.jpg`。
- 前端展示使用后端返回的访问 URL，例如 `/profile/merchant_images/1/product/101/main_xxx.jpg`。
- 后续如果切换 OSS，只需要替换上传服务和 URL 生成逻辑，后台 UI 不需要再改。

---

## 2026-05-25 商家端结算中心切换为微信 T+1 自动到账 ✅ 已完成

### 变更概要
- 商家端 `pages/finance/finance` 从“财务收益 + 手动提现”改为“微信官方 T+1 自动到账结算中心”。
- 新增前端接口方法 `getSettlementOverview`，请求 `GET /wxmini/merchant-mini/settlement/overview`。
- 保留 `getFinanceOverview` 兼容别名，内部转发到新的结算概览接口，降低旧调用迁移风险。
- 移除商家端页面中的提现金额输入、申请提现按钮和手动提现提交流程。
- 页面改为展示待自动到账金额、结算账户、到账记录和订单结算流水。
- 后端新增 `GET /wxmini/merchant-mini/settlement/overview`，返回结算中心概览数据。
- 后端保留 `GET /wxmini/merchant-mini/finance/overview`，内部返回同一套结算概览数据。
- 后端废弃 `POST /wxmini/merchant-mini/finance/withdraw` 的实际受理逻辑，返回“该版本已切换为微信自动结算，无需商家手动提现”。
- 新增商家端结算 DTO：`MerchantMiniSettlementOverviewDto`、`MerchantMiniSettlementAccountDto`、`MerchantMiniSettlementRecordDto`。
- Mock/服务层补充 `WAITING_T1`、`TRANSFERRING`、`ARRIVED`、`FAILED` 等自动结算状态数据，支撑前端联调。

### 接口调整

| 接口 | 状态 | 说明 |
|------|------|------|
| `GET /wxmini/merchant-mini/settlement/overview` | 新增 | 商家端结算中心正式接口 |
| `GET /wxmini/merchant-mini/finance/overview` | 兼容保留 | 内部返回结算概览，兼容旧版本 |
| `POST /wxmini/merchant-mini/finance/withdraw` | 废弃 | 不再受理手动提现 |

### 前端展示字段
- 顶部统计：今日收益、本月收益、待 T+1 结算、打款中、已到账、待自动到账、平台佣金。
- 结算账户：结算主体、银行、银行卡尾号、认证状态。
- 到账记录：结算 ID、订单号、标题、金额、状态、进入结算时间、预计打款时间、到账时间、备注。
- 订单流水：订单金额、商家分账、平台佣金、结算状态、核销完成时间、预计打款时间。

---

## 2026-05-25 商品图片体系 + 核销码体系 + 核销记录（完整商业级设计方案） ✅ 已完成

### 变更概要
- 新增 `product_image` 商品图片表（多图、排序、类型分类、软删除）
- 新增 `write_off_record` 核销记录表（商家/门店/操作员/核销码全链路记录）
- 改造 `product` 表：补充 main_image、sales、stock、status、verify_type 字段
- 改造 `mall_order` 表：补充 write_off_status、write_off_time、write_off_user_id 字段
- 设计核销码生成方案（LY+日期+序列号，Redis原子自增防重复）
- 设计文件上传目录结构（按商家ID/商品ID/图片类型三级分类）
- 设计后端上传接口（兼容本地存储 + 未来OSS扩展）
- 设计商家扫码核销完整链路（时序图 + 状态机 + API）

---

### 一、文件目录结构设计

#### 1.1 上传基路径

配置项：`ruoyi.profile = D:/ruoyi/uploadPath`

#### 1.2 目录结构

```
D:/ruoyi/uploadPath/
├── merchant/                          # 商家相关
│   └── {merchantId}/                  # 按商家ID分类
│       ├── logo/                      # 商家logo
│       │   └── logo_{uuid}.jpg
│       ├── cover/                     # 门店封面
│       │   └── cover_{uuid}.jpg
│       └── product/                   # 商品图片
│           └── {productId}/           # 按商品ID分类
│               ├── main/              # 商品主图
│               │   └── {uuid}.jpg
│               ├── detail/            # 商品详情图
│               │   ├── {uuid}.jpg
│               │   └── {uuid}.jpg
│               └── sku/               # SKU图
│                   └── {uuid}.jpg
├── wxmini/                            # 小程序通用上传（已有）
│   └── {timestamp}_{uuid}.jpg
└── avatar/                            # 用户头像（已有）
```

#### 1.3 上传命名规范

| 图片类型 | 命名规则 | 示例 |
|---------|---------|------|
| 商家logo | `logo_{uuid8}.jpg` | `logo_a1b2c3d4.jpg` |
| 门店封面 | `cover_{uuid8}.jpg` | `cover_e5f6g7h8.jpg` |
| 商品主图 | `main_{uuid8}.jpg` | `main_i9j0k1l2.jpg` |
| 商品详情图 | `detail_{uuid8}.jpg` | `detail_m3n4o5p6.jpg` |
| SKU图 | `sku_{uuid8}.jpg` | `sku_q7r8s9t0.jpg` |

UUID取前8位 + 时间戳前缀，保证：
- **唯一性**：UUID全球唯一
- **不可预测**：随机生成
- **防覆盖**：同名概率极低（1/40亿）

#### 1.4 OSS云存储兼容方案

当前使用本地存储，未来迁移OSS时：

```java
// 抽象存储接口
public interface IFileStorage {
    String upload(byte[] data, String path, String fileName);
    void delete(String path);
    String getUrl(String path);
}

// 本地实现（当前）
@Service
public class LocalFileStorage implements IFileStorage { ... }

// 阿里云OSS实现（未来）
@Service
@ConditionalOnProperty(name = "storage.type", havingValue = "oss")
public class AliOssFileStorage implements IFileStorage { ... }

// 腾讯云COS实现（未来）
@Service
@ConditionalOnProperty(name = "storage.type", havingValue = "cos")
public class TencentCosFileStorage implements IFileStorage { ... }
```

配置切换：
```yaml
# application.yml
storage:
  type: local  # 切换为 oss 即可
  local:
    path: D:/ruoyi/uploadPath
  oss:
    endpoint: oss-cn-shanghai.aliyuncs.com
    bucket: ruoyi-mall
    accessKeyId: xxx
    accessKeySecret: xxx
```

---

### 二、数据库设计

#### 2.1 商品图片表 product_image

```sql
CREATE TABLE `product_image` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '图片ID',
  `product_id` BIGINT NOT NULL COMMENT '商品ID',
  `merchant_id` BIGINT NOT NULL COMMENT '商家ID',
  `image_type` VARCHAR(20) NOT NULL COMMENT '图片类型: main主图/detail详情图/sku',
  `image_url` VARCHAR(500) NOT NULL COMMENT '图片URL路径',
  `sort_order` INT DEFAULT 0 COMMENT '排序号(越小越靠前)',
  `sku_value` VARCHAR(100) DEFAULT NULL COMMENT 'SKU值(如: 红色/蓝色, 仅sku类型时填写)',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0删除 1正常',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_product_id` (`product_id`),
  KEY `idx_merchant_id` (`merchant_id`),
  KEY `idx_product_type` (`product_id`, `image_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品图片表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| product_id | BIGINT | 关联商品表 |
| merchant_id | BIGINT | 冗余商家ID，便于按商家查询 |
| image_type | VARCHAR(20) | main主图 / detail详情图 / sku |
| image_url | VARCHAR(500) | 图片访问路径 /profile/merchant/1/product/1/main/xxx.jpg |
| sort_order | INT | 排序号，详情图按此排序 |
| sku_value | VARCHAR(100) | SKU值，如"红色"，仅image_type=sku时使用 |
| status | TINYINT | 软删除标记 |

**索引设计：**
- `idx_product_id`：按商品查询图片列表
- `idx_merchant_id`：按商家查询所有图片（统计/清理）
- `idx_product_type`：按商品+类型查询（如只查主图）
- `idx_status`：过滤已删除记录

---

#### 2.2 商品表 product 补充字段

```sql
ALTER TABLE `product`
  ADD COLUMN `main_image` VARCHAR(500) DEFAULT NULL COMMENT '商品主图URL(冗余，快速展示用)' AFTER `images`,
  ADD COLUMN `verify_type` TINYINT DEFAULT 1 COMMENT '核销方式: 1在线核销 2到店自提' AFTER `status`,
  ADD INDEX `idx_verify_type` (`verify_type`);
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| main_image | 商品主图URL，冗余存储，列表页快速展示不查product_image表 |
| verify_type | 核销方式：1=在线核销(团购券到店出示二维码)，2=到店自提 |

**注意**：product表已有 `sales`(销量)、`stock`(库存)、`status`(状态) 字段，无需重复添加。

---

#### 2.3 核销记录表 write_off_record

```sql
CREATE TABLE `write_off_record` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `order_id` BIGINT NOT NULL COMMENT '订单ID',
  `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
  `write_off_code` VARCHAR(32) NOT NULL COMMENT '核销码',
  `merchant_id` BIGINT NOT NULL COMMENT '商家ID',
  `store_id` BIGINT DEFAULT NULL COMMENT '核销门店ID',
  `operator_id` BIGINT NOT NULL COMMENT '操作员ID(商家用户表ID)',
  `operator_name` VARCHAR(64) DEFAULT '' COMMENT '操作员姓名',
  `write_off_type` TINYINT DEFAULT 1 COMMENT '核销方式: 1扫码核销 2手动核销',
  `write_off_time` DATETIME NOT NULL COMMENT '核销时间',
  `product_name` VARCHAR(200) DEFAULT '' COMMENT '商品名称(冗余)',
  `product_amount` DECIMAL(10,2) DEFAULT 0.00 COMMENT '商品金额(冗余)',
  `remark` VARCHAR(500) DEFAULT '' COMMENT '备注',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 1有效 0作废',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_write_off_code` (`write_off_code`),
  KEY `idx_order_id` (`order_id`),
  KEY `idx_order_no` (`order_no`),
  KEY `idx_merchant_id` (`merchant_id`),
  KEY `idx_operator_id` (`operator_id`),
  KEY `idx_write_off_time` (`write_off_time`),
  KEY `idx_merchant_time` (`merchant_id`, `write_off_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='核销记录表';
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| order_id / order_no | 关联订单（冗余订单号，便于查询） |
| write_off_code | 核销码，唯一索引，防重复核销 |
| merchant_id | 商家ID，按商家查询核销记录 |
| store_id | 核销门店（多门店商家） |
| operator_id / operator_name | 谁执行的核销操作 |
| write_off_type | 1=扫码核销 2=手动输入核销码核销 |
| product_name / product_amount | 冗余商品信息，核销记录列表直接展示 |

**索引设计：**
- `uk_write_off_code`：核销码唯一，防重复核销
- `idx_merchant_time`：商家按时间查核销记录（最常用查询）

---

#### 2.4 订单表 mall_order 补充字段

```sql
ALTER TABLE `mall_order`
  ADD COLUMN `write_off_status` TINYINT DEFAULT 0 COMMENT '核销状态: 0未核销 1已核销' AFTER `write_off_code`,
  ADD COLUMN `write_off_time` DATETIME DEFAULT NULL COMMENT '核销时间' AFTER `write_off_status`,
  ADD COLUMN `write_off_user_id` BIGINT DEFAULT NULL COMMENT '核销操作员ID' AFTER `write_off_time`,
  ADD INDEX `idx_write_off_status` (`write_off_status`);
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| write_off_status | 0=未核销 1=已核销（独立于订单主状态） |
| write_off_time | 核销时间 |
| write_off_user_id | 核销操作员ID |

**注意**：`write_off_code` 字段已存在于 MallOrder 实体中，无需重复添加。

---

### 三、核销码体系设计

#### 3.1 核销码生成规则

**格式**：`LY{日期8位}{序列号4位}`

**示例**：`LY202605250001`

| 部分 | 说明 | 示例 |
|------|------|------|
| LY | 固定前缀（零点科技团购） | LY |
| 20260525 | 支付日期（8位） | 20260525 |
| 0001 | 当日序列号（4位，Redis原子自增） | 0001 |

**特点**：
- 唯一性：日期+Redis原子自增，不可能重复
- 不可预测：序列号连续但无规律可循
- 可读性：客服可直接读出"LY二零二六零五二五零零零一"
- 可追溯：前8位直接知道支付日期

#### 3.2 Java生成代码

```java
@Service
public class WriteOffCodeGenerator {

    @Resource
    private StringRedisTemplate stringRedisTemplate;

    private static final String CODE_PREFIX = "LY";
    private static final String REDIS_KEY_PREFIX = "write_off:code:seq:";
    private static final int CODE_LENGTH = 4; // 序列号位数

    /**
     * 生成核销码
     * 格式: LY202605250001
     */
    public String generate() {
        String dateStr = new SimpleDateFormat("yyyyMMdd").format(new Date());
        String redisKey = REDIS_KEY_PREFIX + dateStr;

        // Redis原子自增，当日首次从1开始
        Long seq = stringRedisTemplate.opsForValue().increment(redisKey);

        // 设置过期时间（2天后自动清理，防内存泄漏）
        if (seq != null && seq == 1) {
            stringRedisTemplate.expire(redisKey, 2, TimeUnit.DAYS);
        }

        // 格式化为4位数字，左补零
        String seqStr = String.format("%0" + CODE_LENGTH + "d", seq);

        return CODE_PREFIX + dateStr + seqStr;
    }

    /**
     * 校验核销码格式
     */
    public boolean isValid(String code) {
        if (code == null || code.length() != 14) {
            return false;
        }
        if (!code.startsWith(CODE_PREFIX)) {
            return false;
        }
        // 日期部分必须是数字
        String datePart = code.substring(2, 10);
        String seqPart = code.substring(10);
        return datePart.matches("\\d{8}") && seqPart.matches("\\d{4}");
    }
}
```

#### 3.3 Redis防重复方案

```
Redis Key: write_off:code:seq:20260525
Value: 原子自增整数
TTL: 2天（自动过期清理）
```

**流程**：
1. 支付成功时调用 `generate()`
2. Redis `INCR write_off:code:seq:20260525` → 返回序列号
3. 如果返回1（当天首个），设置 `EXPIRE` 2天
4. 拼接 `LY + 日期 + 序列号` 返回

**高并发保障**：
- Redis INCR 是原子操作，10000并发也保证不重复
- 每天独立序列，跨天自动从1开始
- 2天TTL自动清理历史数据

---

### 四、核销流程设计

#### 4.1 完整时序图

```
C端用户                商家端小程序              后端服务               数据库
  │                      │                      │                      │
  │  1. 查看订单详情      │                      │                      │
  │ ──────────────────────────────────────────────────────────────────→│
  │                      │                      │  查询订单+核销码      │
  │ ←──────────────────────────────────────────────────────────────────│
  │  返回订单+核销码      │                      │                      │
  │                      │                      │                      │
  │  2. 展示二维码        │                      │                      │
  │  (内容=核销码)        │                      │                      │
  │                      │                      │                      │
  │  3. 出示二维码 ──────→│  商家扫码             │                      │
  │                      │  解析出核销码          │                      │
  │                      │                      │                      │
  │                      │  4. POST /write-off/{code}                  │
  │                      │ ───────────────────→ │                      │
  │                      │                      │  5. 查询订单          │
  │                      │                      │ ────────────────────→│
  │                      │                      │ ←────────────────────│
  │                      │                      │                      │
  │                      │                      │  6. 校验订单状态      │
  │                      │                      │  - 是否已支付         │
  │                      │                      │  - 是否已核销         │
  │                      │                      │  - 是否已过期         │
  │                      │                      │  - 商家是否匹配       │
  │                      │                      │                      │
  │                      │                      │  7. 更新订单状态      │
  │                      │                      │  write_off_status=1  │
  │                      │                      │ ────────────────────→│
  │                      │                      │                      │
  │                      │                      │  8. 写入核销记录      │
  │                      │                      │ ────────────────────→│
  │                      │                      │                      │
  │                      │  9. 返回核销成功      │                      │
  │                      │ ←─────────────────── │                      │
  │                      │                      │                      │
  │  10. 更新页面状态     │                      │                      │
  │ ←────────────────────│                      │                      │
```

#### 4.2 订单状态机

```
                    ┌─────────────┐
                    │  0 待支付    │
                    └──────┬──────┘
                           │ 用户支付成功
                           ↓
                    ┌─────────────┐
                    │  1 已支付    │  ← 生成核销码 writeOffCode
                    │  (待核销)    │     writeOffStatus = 0
                    └──────┬──────┘
                    │      │
          用户申请退款     │ 商家扫码核销
                    ↓      ↓
            ┌──────────┐  ┌─────────────┐
            │ 4 已退款   │  │ 3 已完成     │  writeOffStatus = 1
            └──────────┘  │  (已核销)    │  writeOffTime = 当前时间
                          └─────────────┘
```

**状态值定义：**

| 状态值 | 名称 | 说明 |
|--------|------|------|
| 0 | 待支付 | 用户下单未支付 |
| 1 | 已支付 | 支付成功，待核销 |
| 3 | 已完成 | 商家已核销 |
| 4 | 已退款 | 用户申请退款 |

**核销前置校验：**
1. 订单是否存在
2. 订单状态是否为 1（已支付）
3. write_off_code 是否与传入的 code 匹配
4. write_off_status 是否为 0（未核销）
5. 订单商家ID 是否与当前操作员的商家ID匹配
6. 商品是否已过期（valid_days）

---

### 五、后端接口设计

#### 5.1 图片上传接口

**接口地址**：`POST /wxmini/common/upload`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | MultipartFile | 是 | 上传文件 |
| merchantId | Long | 是 | 商家ID |
| productId | Long | 否 | 商品ID（商品图片时传） |
| imageType | String | 否 | 图片类型: main/detail/sku/logo/cover |

**返回结构**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "url": "/profile/merchant/1/product/100/main/a1b2c3d4.jpg",
    "fileName": "a1b2c3d4.jpg",
    "imageType": "main"
  }
}
```

**Controller 实现**：

```java
@RestController
@RequestMapping("/wxmini/common")
public class WxMiniUploadController {

    @Value("${ruoyi.profile}")
    private String profilePath;

    @Resource
    private IProductImageService productImageService;

    @PostMapping("/upload")
    public AjaxResult upload(
            @RequestParam("file") MultipartFile file,
            @RequestParam("merchantId") Long merchantId,
            @RequestParam(value = "productId", required = false) Long productId,
            @RequestParam(value = "imageType", required = false, defaultValue = "main") String imageType) {

        if (file == null || file.isEmpty()) {
            return AjaxResult.error("请选择要上传的文件");
        }

        // 校验文件大小（10MB）
        if (file.getSize() > 10 * 1024 * 1024) {
            return AjaxResult.error("文件大小不能超过10MB");
        }

        // 校验文件类型
        String originalName = file.getOriginalFilename();
        String ext = getExtension(originalName);
        if (!isImageExtension(ext)) {
            return AjaxResult.error("仅支持jpg/png/gif格式");
        }

        try {
            // 构建目录: merchant/{merchantId}/{type}/
            // 或: merchant/{merchantId}/product/{productId}/{type}/
            StringBuilder dirBuilder = new StringBuilder("merchant/");
            dirBuilder.append(merchantId).append("/");

            if (productId != null) {
                dirBuilder.append("product/").append(productId).append("/");
            }
            dirBuilder.append(imageType).append("/");

            String subDir = dirBuilder.toString();
            File dir = new File(profilePath, subDir);
            if (!dir.exists()) {
                dir.mkdirs();
            }

            // 生成文件名: {type}_{uuid8}.jpg
            String uuid = UUID.randomUUID().toString().replace("-", "").substring(0, 8);
            String fileName = imageType + "_" + uuid + "." + ext;
            String relativePath = subDir + fileName;

            // 保存文件
            File dest = new File(profilePath, relativePath);
            file.transferTo(dest);

            String url = "/profile/" + relativePath;

            // 如果传了productId，写入product_image表
            if (productId != null) {
                ProductImage image = new ProductImage();
                image.setProductId(productId);
                image.setMerchantId(merchantId);
                image.setImageType(imageType);
                image.setImageUrl(url);
                image.setStatus(1);
                productImageService.insertProductImage(image);
            }

            Map<String, Object> result = new HashMap<>();
            result.put("url", url);
            result.put("fileName", fileName);
            result.put("imageType", imageType);
            return AjaxResult.success(result);

        } catch (IOException e) {
            return AjaxResult.error("文件上传失败: " + e.getMessage());
        }
    }

    private boolean isImageExtension(String ext) {
        return "jpg".equalsIgnoreCase(ext) || "jpeg".equalsIgnoreCase(ext)
            || "png".equalsIgnoreCase(ext) || "gif".equalsIgnoreCase(ext);
    }

    private String getExtension(String fileName) {
        if (fileName == null || !fileName.contains(".")) {
            return "jpg";
        }
        return fileName.substring(fileName.lastIndexOf(".") + 1);
    }
}
```

---

#### 5.2 核销接口

**接口地址**：`POST /wxmini/merchant-mini/order/write-off/{code}`

**请求头**：
| Header | 说明 |
|--------|------|
| Wx-Authorization | Bearer {token}（商家端JWT） |

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| code | String | 核销码（如 LY202605250001） |

**返回结构（成功）**：
```json
{
  "code": 200,
  "msg": "核销成功",
  "data": {
    "orderNo": "20260525000001",
    "productName": "双人套餐",
    "payAmount": 99.00,
    "writeOffTime": "2026-05-25 14:30:00",
    "operatorName": "张三"
  }
}
```

**返回结构（失败）**：
```json
{
  "code": 500,
  "msg": "该订单已核销，请勿重复操作"
}
```

**Controller 实现**：

```java
@PostMapping("/order/write-off/{code}")
public AjaxResult writeOff(@PathVariable String code) {
    // 权限校验
    if (!WxMiniUserContext.isMerchantStaff()) {
        return AjaxResult.error(HttpStatus.FORBIDDEN, "请先登录");
    }
    if (!WxMiniUserContext.hasAnyPermission("verify.scan", "verify.manual")) {
        return AjaxResult.error(HttpStatus.FORBIDDEN, "无核销权限");
    }

    try {
        Long merchantId = WxMiniUserContext.getCurrentMerchantId();
        Long operatorId = WxMiniUserContext.getCurrentUserId();
        String operatorName = WxMiniUserContext.getCurrentUserName();

        WriteOffResultVO result = writeOffService.writeOff(
            code, merchantId, operatorId, operatorName);
        return AjaxResult.success("核销成功", result);
    } catch (BusinessException e) {
        return AjaxResult.error(e.getMessage());
    }
}
```

**Service 实现**：

```java
@Service
public class WriteOffServiceImpl implements IWriteOffService {

    @Resource
    private MallOrderMapper orderMapper;
    @Resource
    private WriteOffRecordMapper writeOffRecordMapper;
    @Resource
    private WriteOffCodeGenerator codeGenerator;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public WriteOffResultVO writeOff(String code, Long merchantId,
                                      Long operatorId, String operatorName) {
        // 1. 校验核销码格式
        if (!codeGenerator.isValid(code)) {
            throw new BusinessException("核销码格式不正确");
        }

        // 2. 查询订单
        MallOrder order = orderMapper.selectOrderByWriteOffCode(code);
        if (order == null) {
            throw new BusinessException("核销码不存在");
        }

        // 3. 校验商家归属
        if (!order.getMerchantId().equals(merchantId)) {
            throw new BusinessException("该订单不属于当前商家");
        }

        // 4. 校验订单状态
        if (order.getStatus() != 1) {
            throw new BusinessException("订单状态异常，无法核销");
        }

        // 5. 校验是否已核销
        if (order.getWriteOffStatus() != null && order.getWriteOffStatus() == 1) {
            throw new BusinessException("该订单已核销，请勿重复操作");
        }

        // 6. 校验是否过期（商品有效天数）
        if (order.getPayTime() != null && order.getValidDays() != null && order.getValidDays() > 0) {
            Date expireTime = DateUtils.addDays(order.getPayTime(), order.getValidDays());
            if (new Date().after(expireTime)) {
                throw new BusinessException("该订单已过期，无法核销");
            }
        }

        // 7. 更新订单状态
        order.setWriteOffStatus(1);
        order.setWriteOffTime(new Date());
        order.setWriteOffUserId(operatorId);
        order.setStatus(3); // 已完成
        order.setUseTime(new Date());
        orderMapper.updateMallOrder(order);

        // 8. 写入核销记录
        WriteOffRecord record = new WriteOffRecord();
        record.setOrderId(order.getId());
        record.setOrderNo(order.getOrderNo());
        record.setWriteOffCode(code);
        record.setMerchantId(merchantId);
        record.setStoreId(order.getStoreId());
        record.setOperatorId(operatorId);
        record.setOperatorName(operatorName);
        record.setWriteOffType(1); // 扫码核销
        record.setWriteOffTime(new Date());
        record.setProductName(order.getProductName());
        record.setProductAmount(order.getPayAmount());
        record.setStatus(1);
        writeOffRecordMapper.insertWriteOffRecord(record);

        // 9. 返回结果
        WriteOffResultVO vo = new WriteOffResultVO();
        vo.setOrderNo(order.getOrderNo());
        vo.setProductName(order.getProductName());
        vo.setPayAmount(order.getPayAmount());
        vo.setWriteOffTime(record.getWriteOffTime());
        vo.setOperatorName(operatorName);
        return vo;
    }
}
```

---

### 六、实体类设计

#### 6.1 ProductImage 实体

```java
package com.ruoyi.mall.product.domain;

import com.ruoyi.common.core.domain.BaseEntity;
import java.util.Date;

public class ProductImage extends BaseEntity {
    private static final long serialVersionUID = 1L;

    private Long id;
    private Long productId;
    private Long merchantId;
    private String imageType;    // main/detail/sku
    private String imageUrl;
    private Integer sortOrder;
    private String skuValue;
    private Integer status;

    // getter/setter 省略
}
```

#### 6.2 WriteOffRecord 实体

```java
package com.ruoyi.mall.order.domain;

import com.ruoyi.common.core.domain.BaseEntity;
import java.math.BigDecimal;
import java.util.Date;

public class WriteOffRecord extends BaseEntity {
    private static final long serialVersionUID = 1L;

    private Long id;
    private Long orderId;
    private String orderNo;
    private String writeOffCode;
    private Long merchantId;
    private Long storeId;
    private Long operatorId;
    private String operatorName;
    private Integer writeOffType;   // 1扫码 2手动
    private Date writeOffTime;
    private String productName;
    private BigDecimal productAmount;
    private String remark;
    private Integer status;

    // getter/setter 省略
}
```

#### 6.3 WriteOffResultVO 返回对象

```java
package com.ruoyi.mall.order.vo;

import java.math.BigDecimal;
import java.util.Date;

public class WriteOffResultVO {
    private String orderNo;
    private String productName;
    private BigDecimal payAmount;
    private Date writeOffTime;
    private String operatorName;

    // getter/setter 省略
}
```

---

### 七、Mapper 设计

#### 7.1 ProductImageMapper

```java
public interface ProductImageMapper {
    ProductImage selectProductImageById(Long id);
    List<ProductImage> selectProductImageList(ProductImage query);
    List<ProductImage> selectByProductId(Long productId);
    List<ProductImage> selectByProductIdAndType(@Param("productId") Long productId,
                                                  @Param("imageType") String imageType);
    int insertProductImage(ProductImage image);
    int updateProductImage(ProductImage image);
    int deleteProductImageById(Long id);  // 逻辑删除: status=0
    int deleteByProductId(Long productId); // 删除商品所有图片
}
```

**核心SQL（ProductImageMapper.xml）**：

```xml
<select id="selectByProductId" resultMap="ProductImageResult">
    SELECT id, product_id, merchant_id, image_type, image_url,
           sort_order, sku_value, status, create_time
    FROM product_image
    WHERE product_id = #{productId} AND status = 1
    ORDER BY sort_order ASC, create_time ASC
</select>

<select id="selectByProductIdAndType" resultMap="ProductImageResult">
    SELECT id, product_id, merchant_id, image_type, image_url,
           sort_order, sku_value, status, create_time
    FROM product_image
    WHERE product_id = #{productId}
      AND image_type = #{imageType}
      AND status = 1
    ORDER BY sort_order ASC
</select>
```

#### 7.2 WriteOffRecordMapper

```java
public interface WriteOffRecordMapper {
    WriteOffRecord selectWriteOffRecordById(Long id);
    List<WriteOffRecord> selectWriteOffRecordList(WriteOffRecord query);
    List<WriteOffRecord> selectByMerchantId(@Param("merchantId") Long merchantId);
    int insertWriteOffRecord(WriteOffRecord record);
    WriteOffRecord selectByWriteOffCode(String writeOffCode);
}
```

#### 7.3 MallOrderMapper 新增方法

```java
// 新增方法
MallOrder selectOrderByWriteOffCode(String writeOffCode);
```

**对应SQL**：
```xml
<select id="selectOrderByWriteOffCode" resultMap="MallOrderResult">
    SELECT * FROM mall_order
    WHERE write_off_code = #{code} AND status != 0
</select>
```

---

### 八、小程序二维码方案

#### 8.1 二维码内容

二维码内容**仅保存核销码**，不存JSON：
```
LY202605250001
```

**原因**：
- 小程序码有长度限制
- 纯文本解析最快
- 不暴露订单金额等敏感信息

#### 8.2 C端生成方案（Canvas绘制）

```javascript
// wx_app/pages/order-detail/order-detail.js

// 方案A：使用小程序Canvas绘制二维码
// 需要引入 weapp-qrcode.js 库

const QRCode = require('../../utils/weapp-qrcode');

Page({
  data: {
    qrcodeReady: false
  },

  drawQRCode(writeOffCode) {
    const that = this;
    const ctx = wx.createCanvasContext('qrcodeCanvas', this);

    QRCode.api.draw({
      width: 200,
      height: 200,
      canvasId: 'qrcodeCanvas',
      text: writeOffCode,  // 仅核销码
      callback: () => {
        that.setData({ qrcodeReady: true });
      }
    }, that);
  },

  // 保存二维码到相册
  saveQRCode() {
    wx.canvasToTempFilePath({
      canvasId: 'qrcodeCanvas',
      success(res) {
        wx.saveImageToPhotosAlbum({
          filePath: res.tempFilePath,
          success() {
            wx.showToast({ title: '已保存到相册' });
          }
        });
      }
    }, this);
  }
});
```

#### 8.3 方案B：后端生成小程序码（推荐）

```java
// 后端生成小程序码接口
@GetMapping("/wxmini/order/qrcode/{orderNo}")
public AjaxResult generateQRCode(@PathVariable String orderNo) {
    MallOrder order = orderMapper.selectOrderByOrderNo(orderNo);
    if (order == null) {
        return AjaxResult.error("订单不存在");
    }
    if (order.getWriteOffCode() == null) {
        return AjaxResult.error("该订单暂无核销码");
    }

    // 调用微信接口生成小程序码
    // scene = 核销码, page = 商家端扫码页
    byte[] qrBytes = wxMaService.getQrcodeService()
        .createQrcode(order.getWriteOffCode(), "pages/verify/verify");

    // 上传到服务器
    String path = "qrcode/" + orderNo + ".jpg";
    // ... 保存文件

    return AjaxResult.success(url);
}
```

#### 8.4 安全方案

| 风险 | 防护措施 |
|------|---------|
| 二维码截图转发 | 核销码绑定订单，同一码只能核销一次 |
| 暴力猜解 | 核销码14位，组合数40亿+，不可能猜解 |
| 跨商家核销 | 后端校验商家归属，不匹配直接拒绝 |
| 重复核销 | write_off_code 唯一索引 + 代码双重校验 |
| 过期订单 | 校验 valid_days，过期拒绝核销 |

---

### 九、文件变更清单

#### 后端新增文件

| 文件 | 说明 |
|------|------|
| ruoyi-mall-product/domain/ProductImage.java | 商品图片实体 |
| ruoyi-mall-product/mapper/ProductImageMapper.java | 商品图片Mapper |
| ruoyi-mall-product/mapper/ProductImageMapper.xml | 商品图片SQL |
| ruoyi-mall-product/service/IProductImageService.java | 商品图片Service接口 |
| ruoyi-mall-product/service/ProductImageServiceImpl.java | 商品图片Service实现 |
| ruoyi-mall-order/domain/WriteOffRecord.java | 核销记录实体 |
| ruoyi-mall-order/mapper/WriteOffRecordMapper.java | 核销记录Mapper |
| ruoyi-mall-order/mapper/WriteOffRecordMapper.xml | 核销记录SQL |
| ruoyi-mall-order/service/IWriteOffService.java | 核销Service接口 |
| ruoyi-mall-order/service/WriteOffServiceImpl.java | 核销Service实现 |
| ruoyi-mall-order/vo/WriteOffResultVO.java | 核销结果VO |
| ruoyi-mall-common/service/WriteOffCodeGenerator.java | 核销码生成器 |
| ruoyi-wxmini/controller/WxMiniUploadController.java | 上传接口Controller |

#### 后端修改文件

| 文件 | 修改内容 |
|------|---------|
| MallOrder.java | 新增 writeOffStatus、writeOffTime、writeOffUserId 字段 |
| MallOrderMapper.java | 新增 selectOrderByWriteOffCode 方法 |
| MallOrderMapper.xml | 新增按核销码查询SQL |
| WxOrderController.java | 支付成功后调用WriteOffCodeGenerator生成核销码 |
| MerchantMiniController.java | writeOff接口改用真实WriteOffService |

#### 数据库执行脚本

| 文件 | 说明 |
|------|------|
| 05_product_image.sql | 创建product_image表 |
| 06_write_off_record.sql | 创建write_off_record表 |
| 07_product_alter.sql | product表新增字段 |
| 08_order_alter.sql | mall_order表新增字段 |

---

### 十、后期优化建议

1. **图片CDN加速**：上传后同步到CDN，前端访问CDN URL
2. **图片压缩**：上传时自动生成缩略图（100x100、400x400）
3. **批量上传**：支持一次上传多张图片
4. **图片水印**：自动添加商家水印防盗图
5. **核销码有效期**：超过valid_days自动标记为已过期
6. **批量核销**：支持一次扫码核销多个订单
7. **核销统计**：按日/周/月统计核销量
8. **Redis缓存**：商品图片列表缓存，减少数据库查询

---

### 变更概要
- `merchant/home` 接口在 `c_mini_app_id` 为 NULL 时不再报错，自动回退到第一个活跃商家
- 修复 `OrderItemMapper.xml`：`order_item` 表无 `update_time`/`remark` 列，从 resultMap/SELECT 中移除
- 修复 `RefundRecordMapper.xml`：`refund_record` 表无 `remark` 列，从 resultMap/SELECT 中移除
- `WxMiniJwtFilter` 白名单新增 `/wxmini/merchant/home`

### 后端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| WxMerchantController.java | 修改 | home() 增加回退逻辑：c_mini_app_id 查不到时，selectMerchantList(status=1) 取第一条 |
| WxMiniJwtFilter.java | 修改 | checkIsExcludeUri() 新增 `/wxmini/merchant/home` |
| OrderItemMapper.xml | 修改 | 移除 `update_time`/`remark`（DB 表无此列） |
| RefundRecordMapper.xml | 修改 | 移除 `remark`（DB 表无此列） |

---

## 2026-05-24 首页接口合并 + baseUrl 改本地回环

### 变更概要
- 后端新增 `GET /wxmini/merchant/home?appid=xxx` 接口，按小程序 AppID 一次返回商家信息 + 所有上架商品
- 前端首页从两次 API 调用（merchant/list + groupon/list）简化为一次 getMerchantHome 调用
- 精简 home.js，删除 merchantList/displayMerchantList 相关逻辑
- wx_app 和 merchant_app 的 baseUrl 改为 localhost（后端、数据库、小程序全在同一台机器）

### 后端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| WxMerchantController.java | 新增 | `GET /wxmini/merchant/home?appid=xxx`：按 c_mini_app_id 查商家 → 查主门店 → 查上架商品 → 返回 {merchant, products} |

### 前端变更（wx_app）

| 文件 | 操作 | 说明 |
|------|------|------|
| api/merchant.js | 修改 | 新增 `getMerchantHome(appid)` 函数 |
| pages/home/home.js | 重写 | `loadData()` 从 Promise.all(两次调用) 改为单次 `api.getMerchantHome(appid)`；删除 merchantList/displayMerchantList；新增 `calcDistance` 前端距离计算 |

### 接口说明
```
GET /wxmini/merchant/home?appid=wx6c708117ea8eaab4
→ 按 c_mini_app_id 查 merchant 表 → 查 merchant_store 主门店 → 查 product 上架商品
→ 返回 { merchant: WxMerchantItemDto, products: [WxGrouponItemDto] }
```

### 登录说明
- C端登录：`mine.js` → `wx.login()` 拿 code → `GET /wxmini/login?appid=xxx&code=yyy`
- 后端按 AppID 查 `merchant.c_mini_app_secret` → 调微信 SDK 换 openId → 返回 token
- 前提：merchant 表需配置 `c_mini_app_id` 和 `c_mini_app_secret`

---

## 2026-05-24 前后端清理演示数据 + 修复 NaN 和 remark 报错

### 变更概要
- 删除 `merchant_app` 和 `wx_app` 中所有 mock/演示数据及相关逻辑
- 商家端 util.js 从 573 行精简至 ~100 行，移除 20+ 模拟业务函数
- wx_app 删除 `data/mock.js`、`data/` 目录、`mock-template-source.js`、`pages/home_page/`
- 修复后端 `MerchantStoreMapper.xml` 引用不存在的 `remark` 列导致的 SQL 错误
- 修复前端 API 返回空数据时产生 `NaN` 的问题（`parseInt(undefined)` / 硬编码兜底 ID）

### 后端修复

| 文件 | 操作 | 说明 |
|------|------|------|
| MerchantStoreMapper.xml | 修改 | 移除 resultMap/SELECT/INSERT/UPDATE 中所有 `remark` 引用（DB 表无该列） |

### 前端变更 — 商家端 merchant_app

| 文件 | 操作 | 说明 |
|------|------|------|
| data/mock.js | 删除 | 286 行 mock 数据（permissionOptions, roleTemplates, staffList, merchantInfo 等） |
| app.js | 修改 | 删除 `require('./data/mock')` 和 `util.initMerchantMockStorage(mock)` |
| utils/util.js | 重写 | 删除 initMerchantMockStorage、6 组 localStorage CRUD、20+ 业务函数；保留 formatDate/formatPrice/showToast/showModal/导航/状态映射 |
| pages/workbench/workbench.js | 修改 | 删除 loadLocalData()；.catch() 只显示错误 toast |
| pages/order/order.js | 修改 | 移除 util.getOrderList() 回退；操作失败只显示错误 |
| pages/order-detail/order-detail.js | 修改 | 移除本地回退；操作失败只显示错误 |
| pages/goods/goods.js | 修改 | 移除 util.getGoodsList() 回退 |
| pages/goods-edit/goods-edit.js | 修改 | 删除 syncLocalGoods() 和本地保存逻辑 |
| pages/verify/verify.js | 修改 | 移除 util.getOrderList() 查 hint |
| pages/verify-records/verify-records.js | 修改 | 移除本地回退 |
| pages/finance/finance.js | 修改 | 移除 util.buildFinanceOverview() 回退 |
| pages/store/store.js | 修改 | 移除 util.getStoreInfo() 回退 |
| pages/staff/staff.js | 修改 | permissionOptions 改为内联常量 |
| pages/mine/mine.js | 修改 | 移除 util.getStoreInfo() 调用 |
| pages/marketing/marketing.js | 修改 | 删除 initMockCoupons/initMockPromotions；移除本地 localStorage 逻辑 |
| pages/apply/apply.js | 修改 | 删除 localStorage 模拟提交 |

### 前端变更 — C 端 wx_app

| 文件 | 操作 | 说明 |
|------|------|------|
| data/mock.js | 删除 | 330 行 mock 数据 |
| data/ 目录 | 删除 | 整个目录 |
| services/template/mock-template-source.js | 删除 | mock 品牌配置 |
| pages/home_page/ 目录 | 删除 | 未使用的模板页面 |
| app.js | 修改 | 移除 util.getStoredOrderList() 调用 |
| utils/util.js | 重写 | 删除 getStoredOrderList/setStoredOrderList/clone/订单状态流转函数 |
| services/template/template-service.js | 修改 | 移除 mock-template-source 导入 |
| pages/home/home.js | 修改 | API 失败显示空列表 + 错误 toast；加 ID 守卫防 NaN |
| pages/mine/mine.js | 修改 | 移除 mock 订单/优惠券/收藏计数 |
| pages/order/order.js | 修改 | 移除 ensureOrderStorage/loadOrdersLocal/updateOrderList |
| pages/order-detail/order-detail.js | 修改 | 移除 updateOrder() 本地状态管理 |
| pages/checkout/checkout.js | 修改 | 移除 submitOrderLocal/mock 回退；加 ID 守卫 |
| pages/product-detail/product-detail.js | 修改 | API 失败显示空页；加 ID 守卫 |
| pages/merchant-detail/merchant-detail.js | 修改 | API 失败显示空页；加 ID 守卫 |
| pages/search/search.js | 修改 | hotList 清空 |
| pages/search-result/search-result.js | 修改 | 通过 API 搜索 |
| pages/coupon/coupon.js | 修改 | 通过 API 加载 |
| pages/favorite/favorite.js | 修改 | 显示空列表，提示"收藏功能需要后端支持" |
| pages/review/review.js | 修改 | 通过 API 加载订单信息 |

### NaN 问题修复

| 文件 | 问题 | 修复 |
|------|------|------|
| home.js | `merchantList[0].id` 在空数组时为 undefined | `onMerchantTap`/`goMerchantDetail` 等加 `if (!id) return` |
| merchant-detail.js | `parseInt(undefined || 1)` 硬编码兜底 | 无 ID 时提示"商家不存在"并返回 |
| product-detail.js | `parseInt(undefined || 101)` 硬编码兜底 | 无 ID 时提示"商品不存在"并返回 |
| checkout.js | `parseInt(undefined || 101)` 硬编码兜底 | 无 ID 时提示"商品不存在"并返回 |

### 未改动文件
- `utils/request.js` — HTTP 客户端，无 mock
- `api/*.js` — API 接口层，无 mock
- `services/cart.js` — 独立 localStorage，无 mock
- 所有 `.wxml` / `.wxss` — 不涉及 mock

---

## 2026-05-19 商家详情页加入小程序配置（AppID/Secret编辑）

### 变更概要
- 商家详情页"基本信息"tab新增"小程序配置"展示区
- 显示C端AppID/Secret、商家端AppID/Secret、微信商户号/API密钥
- Secret和API密钥显示为`******`，未配置显示"未配置"
- "编辑配置"弹窗可修改所有6个字段，保存后自动刷新
- 后端无改动，已有字段和update接口完全支持

### 前端变更（运营后台）

| 文件 | 操作 | 说明 |
|------|------|------|
| merchant/detail.vue | 修改 | 基本信息tab新增小程序配置展示 + 编辑弹窗 + handleEditMiniApp/submitMiniAppForm |

### 多租户联动逻辑（已有，无需改动）
- C端登录：前端传AppID → 后端`WxMaServiceManager.getService(appId)` → 按AppID查merchant表获取secret → 返回对应商家的用户数据
- 商家端登录：用户名密码 → JWT含merchantId → 所有接口按merchantId隔离数据
- 不同商家配置不同AppID → 后端自动路由到对应商家

---

## 2026-05-19 商家账户管理（后台详情页 + 自动创建owner）

### 变更概要
- 创建商家时自动创建一个默认owner账号（密码123456，BCrypt加密）
- 商家详情页新增"账户管理"tab，支持创建账号/重置密码/启用禁用
- 商家端只能用后台创建的账号登录，不走微信登录

### 后端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| MerchantServiceImpl.java | 修改 | insertMerchant()加@Transactional, 插入商家后自动创建owner账号(用户名优先用手机号, 默认密码123456) |

### 前端变更（运营后台）

| 文件 | 操作 | 说明 |
|------|------|------|
| merchant/detail.vue | 修改 | 新增"账户管理"tab: 账户列表表格 + 新增账户弹窗 + 重置密码/启用禁用按钮 |

### 逻辑说明
- 创建商家 → 自动创建owner账号（username=手机号, password=123456, role=owner, status=1）
- 详情页"账户管理"tab → 调用 `/mall/merchant-user/list?merchantId=xxx` 加载该商家所有账号
- 新增账户 → 调用 `POST /mall/merchant-user`，需填用户名/密码/姓名/手机号/角色
- 重置密码 → 调用 `PUT /mall/merchant-user/resetPwd/{id}`，密码重置为123456
- 启用/禁用 → 调用 `PUT /mall/merchant-user/changeStatus`，切换status 0/1

---

## 2026-05-19 商家端登录改造 + 订单删除配送中

### 变更概要
- 商家端登录从"角色卡片选择"改为标准用户名+密码登录，BCrypt密码校验
- 登录响应新增merchantName字段，前端显示商家名称
- 订单管理删除"配送中"状态（tab + 按钮 + 统计卡片）
- 运行了03_multi_tenant_miniapp.sql，merchant表新增6个多租户字段

### 后端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| MerchantMiniLoginRequestDto.java | 重写 | roleKey → username + password |
| MerchantMiniStaffUserDto.java | 修改 | +merchantName字段 |
| MerchantMiniStoreDto.java | 修改 | +merchantName字段 |
| MerchantMiniWorkbenchStatsDto.java | 修改 | +pendingAcceptCount字段 |
| IMerchantMiniMockService.java | 修改 | login(String roleKey) → login(String username, String password) |
| MerchantMiniServiceImpl.java | 修改 | login加BCrypt密码校验, buildStaffUserFromDb加merchantName, buildStoreProfile加merchantName |
| MerchantMiniMockServiceImpl.java | 修改 | 同步login签名, cloneStaffUser/cloneStoreInfo加merchantName |
| MerchantMiniController.java | 修改 | login端点从requestDto取username+password |

### 前端变更（商家端小程序）

| 文件 | 操作 | 说明 |
|------|------|------|
| merchant_app/pages/login/login.js | 重写 | 角色卡片 → 用户名+密码表单, 去掉mock fallback |
| merchant_app/pages/login/login.wxml | 重写 | 新增用户名/密码输入框 |
| merchant_app/pages/login/login.wxss | 重写 | 新增输入框样式, 去掉角色卡片样式 |
| merchant_app/pages/order/order.js | 修改 | tabs删除"配送中", 删除handleShipOrder方法 |
| merchant_app/pages/order/order.wxml | 修改 | 删除SHIPPING/ACCEPTED按钮, ACCEPTED改为"确认完成" |
| merchant_app/pages/workbench/workbench.js | 修改 | 统计卡片删除"配送中" |
| merchant_app/utils/util.js | 修改 | orderStatusMap删除SHIPPING, 删除shipOrder函数, completeOrder改为ACCEPTED/PENDING_VERIFY可操作 |

### 登录流程
```
前端：输入username + password → POST /wxmini/merchant-mini/auth/login {username, password}
后端：查merchant_user表 → BCrypt.matches校验密码 → 查merchant表 → 返回token + staffUser(含merchantName/permissions)
```

### 数据库执行
- 已执行 `03_multi_tenant_miniapp.sql`：merchant表新增c_mini_app_id/secret, m_mini_app_id/secret, wx_pay_mch_id/api_key + 2唯一索引

---

## 2026-05-19 多租户小程序架构（每个商家独立AppID）

### 架构说明
- 每个商家拥有2个独立微信小程序：C端（用户下单）+ 商家端（管理/核销）
- 一个后端服务所有商家，通过 AppID→merchantId 映射隔离数据
- C端登录：前端传 AppId + code → 后端按 AppId 查商家 → 用该商家的 secret 调微信
- 商家端登录：用户名密码登录（JWT含merchantId），不走微信SDK
- 用户体系：每个小程序的用户完全独立（Option A），openId不同小程序互不关联

### 数据库变更
- `merchant` 表新增字段：c_mini_app_id, c_mini_app_secret, m_mini_app_id, m_mini_app_secret, wx_pay_mch_id, wx_pay_api_key
- c_mini_app_id 和 m_mini_app_id 加唯一索引
- SQL脚本：`ruoyi-vue-wxmini-main/project-docs/test-sql/03_multi_tenant_miniapp.sql`

### 后端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| mall-merchant/Merchant.java | 修改 | +cMiniAppId/cMiniAppSecret/mMiniAppId/mMiniAppSecret/wxPayMchId/wxPayApiKey |
| mall-merchant/MerchantMapper.java | 修改 | +selectMerchantByCAppId, +selectMerchantByMAppId |
| mall-merchant/MerchantMapper.xml | 修改 | resultMap/SQL加6个新字段, +两个按AppId查询 |
| mall-common/WxMaServiceManager.java | 新建 | 多租户WeChat SDK管理器, Map<AppId,WxMaService>, register/getService |
| mall-user/WxLoginController.java | 重写 | 注入WxMaServiceManager+MerchantMapper, 按appId从DB加载secret |
| mall-user/WxPortalController.java | 重写 | URL路径带AppId, 从DB加载配置验证签名 |
| mall-user/WxMaUserController.java | 重写 | 注入WxMaServiceManager, 按appId获取service |
| mall-user/pom.xml | 修改 | +ruoyi-mall-merchant依赖(使WxLoginController能注入MerchantMapper) |
| wxmini/WxPortalController.java | 修改 | 禁用@RestController(避免URL冲突, 已移至mall-user) |

### 前端变更

| 文件 | 操作 | 说明 |
|------|------|------|
| ruoyi-ui/views/merchant/add.vue | 修改 | 新增"小程序配置"分组(C端AppID/Secret、商家端AppID/Secret、商户号/API密钥) |
| wx_app/app.js | 修改 | initEnv()通过wx.getAccountInfoSync()自动获取当前小程序AppID |

### 登录流程
```
C端：wx.login() → wx.getAccountInfoSync().miniProgram.appId → POST /wxmini/login?appid=xxx&code=yyy
→ WxLoginController → WxMaServiceManager.getService(appid) → DB查merchant表加载secret
→ wxMaService.getUserService().getSessionInfo(code) → openid → 查mall_user → token

商家端：POST /wxmini/merchant-mini/auth/login {username: xxx}
→ MerchantMiniServiceImpl → 查merchant_user → JWT(含merchantId)
```

### 微信消息推送
每个商家配置独立verifyUrl：`https://domain.com/wxmini/portal/{c_mini_app_id}`
- GET: 微信服务器验证签名
- POST: 接收消息推送（当前无实际处理逻辑，预留）

---

## 2026-05-18 商家用户管理（后台管理商家端账号）

### 需求
- 一个商家下有多个用户：1个超级管理员(owner) + 多个普通成员(member)
- 商家端用户通过运营后台统一管理（创建/编辑/禁用/重置密码）
- 商家端小程序只能查看员工列表，不能创建新账号

### 新增内容
| 层级 | 内容 |
|------|------|
| ruoyi-mall-merchant | IMerchantUserService + MerchantUserServiceImpl（service层） |
| ruoyi-admin | MallMerchantUserController（CRUD + 状态切换 + 重置密码） |
| ruoyi-ui前端 | 商家用户管理页面 + API函数 + 路由注册 |

---

## 2026-05-18 后端接口异常修复

### 修复问题
1. `MallOrderMapper.xml`：`countOrderByMerchantIdAndStatus` 错误使用 `resultMap="MallOrderResult"` 改为 `resultType="int"`（count查询不能用实体映射）
2. `DashboardServiceImpl.selectTrendData()`：`row.get("date")` 返回 `java.sql.Date` 不能强转为 `String`，改用 `SimpleDateFormat.format()` 兼容；同时修正字段名 `count`→`orderCount`、`amount`→`totalAmount`，移除不存在的 `completedCount` 引用
3. `MallOrderMapper.selectOrderStatsByStatus()`：SQL别名 `AS count` 改为 `AS cnt`，与 DashboardServiceImpl 读取 `row.get("cnt")` 匹配
4. `MallOrderMapper`：`selectSalesStats`/`sumTodayAmount`/`sumTotalFlow` 的 `NOT IN (0, 6)` 改为 `NOT IN (0, 5)`（数据库状态最大值为5，6不存在）
5. `MallDashboardExtendController.salesStats()`：SQL返回 `totalAmount` 而非 `totalSales`，修正字段名；`avgOrderAmount` 改为代码计算（总金额/总订单数）而非读取不存在的字段
6. `MallDashboardExtendController`：新增 `/order-status` 和 `/hot-products` 两个缺失端点，前端 dashboard 首页调用这两个接口
7. `ProductMapper.selectHotProducts()`：`m.merchant_name` 改为 `m.name AS merchant_name`（merchant 表字段名为 name）
8. `TransactionRecordMapper.xml`：`transaction_record` 表无 `update_time`/`remark` 列，从 SELECT/INSERT/resultMap 中移除
9. `PlatformIncomeMapper.xml`：`platform_income` 表无 `update_time`/`remark` 列，从 SELECT/INSERT/resultMap 中移除

---

## 2026-05-18 商家端小程序接入真实数据库

### 变更概要
- 新建 `MerchantMiniServiceImpl` 替代 `MerchantMiniMockServiceImpl`，17个接口全部接入真实数据库查询
- ruoyi-wxmini pom.xml 新增依赖：ruoyi-mall-merchant/order/product/finance/user
- 禁用 Mock 服务（注释 @Service 注解）

### 接入详情

| 功能模块 | 数据源 | 说明 |
|---------|--------|------|
| 登录 | merchant_user + merchant | 通过用户名查询商家账号，校验状态，生成JWT |
| 工作台 | mall_order + product + merchant_store | 实时统计待核销/已完成/退款订单数、今日销售额、在售商品数 |
| 订单列表 | mall_order + order_item + mall_user | 按商家ID查询订单，关联商品名和用户昵称 |
| 订单详情 | mall_order + order_item | 按订单号查询，校验商家归属 |
| 核销 | mall_order | 通过订单号/核销码查找，更新订单状态为已完成 |
| 核销记录 | mall_order | 已完成/已使用的订单作为核销记录 |
| 商品列表 | product | 按商家ID查询，状态过滤(上架/下架) |
| 商品管理 | product | 新增/更新商品，上下架操作 |
| 门店信息 | merchant + merchant_store | 查询商家及主门店信息 |
| 员工管理 | merchant_user | 按商家ID查询员工列表，更新状态 |
| 财务概览 | mall_order + withdraw_record | 统计今日/本月收入、可提现金额、分账明细 |
| 提现申请 | withdraw_record | 校验可提现余额，插入提现记录 |

### 状态值映射

| 前端状态 | 数据库值 | 说明 |
|---------|---------|------|
| mall_order: PENDING_VERIFY | 1(已支付)/2(已使用) | 待核销 |
| mall_order: COMPLETED | 3(已完成) | 已核销 |
| mall_order: REFUNDING | 4(已退款) | 退款中 |
| product: ON_SHELF | 1(上架) | 在售 |
| product: OFF_SHELF | 0(下架) | 已下架 |

---

## 2026-05-18 Maven多模块DDD重构（全部完成）

### 变更概要
- 将 `com.ruoyi.wxmini` 包下全部商城业务代码按领域拆分为 8 个 Maven 模块
- 新建模块：ruoyi-mall-common / user / merchant / product / order / finance / marketing / pay
- 清理 ruoyi-wxmini（保留 Mock + 适配层，删除25个已搬走文件）
- 清理 ruoyi-system（删除整个 com.ruoyi.wxmini 包，47 Java + 13 XML）
- 更新 ruoyi-admin（依赖8个mall模块，DashboardServiceImpl搬入）
- 新增 4 个前端缺失接口（dashboard/stats, dashboard/trend, dashboard/merchant-rank, merchant/flow/list）
- 新增 `API接口文档.md`（57个接口完整清单）

### 新建模块清单

| 模块 | 文件数 | 职责 |
|------|--------|------|
| ruoyi-mall-common | 15 | JWT/Filter/Config/Pay基类/IDashboardService |
| ruoyi-mall-user | 17 | 用户域：UserInfo/MallUser/Cart + 登录接口 |
| ruoyi-mall-merchant | 14 | 商家域：Merchant/Store/User + 审核 |
| ruoyi-mall-product | 11 | 商品域：Product/Category/Groupon |
| ruoyi-mall-order | 15 | 订单域：Order/OrderItem/Refund + 售后 |
| ruoyi-mall-finance | 15 | 财务域：Transaction/Withdraw/Income/Bill |
| ruoyi-mall-marketing | 3 | 营销域：Banner/Coupon（预留） |
| ruoyi-mall-pay | 3 | 支付域：PaymentRecord + WxPayController |

### 清理操作

| 模块 | 操作 | 说明 |
|------|------|------|
| ruoyi-wxmini | 删除25文件 | config/bo/filter/service/vo/util/controller，pom改依赖mall-common |
| ruoyi-system | 删除wxmini包 | 47 Java + 13 MyBatis XML |
| ruoyi-admin | 重构 | DashboardServiceImpl搬入，pom依赖8个mall模块，删除3个重复controller，新增4个接口 |

### 修复的编译问题
- 父POM漏注册 ruoyi-mall-order（dependencyManagement + modules）
- DashboardServiceImpl 泛型类型不匹配（Mapper返回raw List<Map>）

### 新增接口

| 接口 | 说明 |
|------|------|
| GET /mall/dashboard/stats | 工作台统计（调用IDashboardService） |
| GET /mall/dashboard/trend | 近7天趋势数据 |
| GET /mall/dashboard/merchant-rank | 商家排行 |
| GET /mall/merchant/flow/list | 商家流水列表（调用IFinanceService） |

### 模块依赖图
```
ruoyi-common → ruoyi-system, ruoyi-mall-common, ruoyi-quartz, ruoyi-generator
ruoyi-mall-common → mall-user, mall-merchant, mall-product, mall-marketing, mall-pay
mall-order → mall-common + mall-product
mall-finance → mall-common + mall-order
ruoyi-admin → 依赖所有mall模块
ruoyi-wxmini → mall-common
```

### 无需修改的配置
- `mapperLocations: classpath*:mapper/**/*Mapper.xml`（已支持多模块）
- `@MapperScan("com.ruoyi.**.mapper")`（已覆盖新包路径）
- `@SpringBootApplication` at com.ruoyi 自动扫描 com.ruoyi.mall.*

---

## 2026-05-14 商家提现/流水页面对接后端 + 财务页面金额类型修复

### 变更概要
- `merchant/withdraw.vue`：修正 API 导入路径，通过/拒绝操作改为调用真实后端接口
- `merchant/flow.vue`：修复字段名 totalAmount→amount，类型筛选值从中文改为英文
- 财务 3 个页面金额显示加 Number() 包装，防止 BigDecimal 序列化报错

---

## 2026-05-12 后端Controller真实SQL实现 + 订单管理 + 前端字段修复

### 变更概要
- MallDashboardExtendController：从硬编码零值改为真实SQL聚合查询
- MallFinanceExtendController：新增平台流水/分账/提现列表接口
- 新建MallOrderExtendController：订单列表/详情/异常/售后接口
- Mapper接口新增7个聚合查询方法（@Select注解）
- 修复3个finance前端页面字段名不匹配

---

## 2026-05-11 后端接入: Domain + Mapper + Service + Controller

### 变更概要
- 完成 21 个 Domain 实体类（映射19张业务表）
- 完成 12 个 Mapper 接口 + 12 个 MyBatis XML
- 完成 6 个 Service 接口 + 6 个 ServiceImpl
- 完成 7 个 Controller（运营后台API）

---

## 2026-05-10 数据库配置 + 前端菜单重构

### 变更概要
- 数据库名统一为 ruoyi-cs
- 前端菜单结构重构（6大模块）
- 新建15张业务表 + 初始化数据
- 新建商家审核/详情页面
