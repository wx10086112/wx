<view class="login-page">
  <view class="hero-block">
<<<<<<< HEAD
    <text class="hero-title">商家端日常操作</text>
    <text class="hero-desc">单店店长 / 店员登录，处理团购订单、到店核销、商品和门店运营。</text>
  </view>

  <view class="role-list">
    <view
      wx:for="{{roleCardList}}"
      wx:key="roleKey"
      class="role-card {{selectedRoleKey === item.roleKey ? 'active' : ''}}"
      bindtap="handleRoleTap"
      data-role="{{item.roleKey}}"
    >
      <text class="role-title">{{item.title}}</text>
      <text class="role-desc">{{item.desc}}</text>
    </view>
  </view>

  <view class="login-panel">
    <text class="panel-label">当前模拟账号</text>
    <text class="panel-value">{{selectedRoleKey === 'manager' ? '林店长 / 店长角色' : '周店员 / 店员角色'}}</text>
    <button class="primary-btn login-btn" bindtap="submitLogin">进入商家端</button>
=======
    <text class="hero-title">商家端登录</text>
  </view>

  <view class="login-panel">
    <view class="input-group">
      <text class="panel-label">账号</text>
      <input class="login-input" placeholder="请输入商家账号" value="{{username}}" bindinput="onUsernameInput" />
    </view>
    <view class="input-group">
      <text class="panel-label">密码</text>
      <input class="login-input" password placeholder="请输入密码" value="{{password}}" bindinput="onPasswordInput" />
    </view>
    <button class="primary-btn login-btn" bindtap="submitLogin">登录</button>
>>>>>>> 苏
    <view class="apply-entry" bindtap="goApply">
      <text>还没有店铺？</text>
      <text class="apply-link">商家入驻申请</text>
    </view>
  </view>
</view>
