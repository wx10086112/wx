<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.user.mapper.MallUserMapper">

    <resultMap type="com.ruoyi.mall.user.domain.MallUser" id="MallUserResult">
        <result property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="city" column="city"/>
        <result property="openId" column="open_id"/>
        <result property="status" column="status"/>
        <result property="totalOrders" column="total_orders"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="selectMallUserVo">
        select id, nickname, phone, avatar, gender, city, open_id, status,
               total_orders, total_amount, create_time, update_time, remark, del_flag
        from mall_user
    </sql>

    <select id="selectMallUserList" parameterType="com.ruoyi.mall.user.domain.MallUser" resultMap="MallUserResult">
        <include refid="selectMallUserVo"/>
        <where>
            and del_flag = '0'
            <if test="nickname != null and nickname != ''">and nickname like concat('%', #{nickname}, '%')</if>
            <if test="phone != null and phone != ''">and phone = #{phone}</if>
            <if test="openId != null and openId != ''">and open_id = #{openId}</if>
            <if test="status != null">and status = #{status}</if>
        </where>
    </select>

    <select id="selectMallUserById" parameterType="Long" resultMap="MallUserResult">
        <include refid="selectMallUserVo"/>
        where id = #{id} and del_flag = '0'
    </select>

    <select id="selectMallUserByOpenId" parameterType="String" resultMap="MallUserResult">
        <include refid="selectMallUserVo"/>
        where open_id = #{openId} and del_flag = '0'
    </select>

    <insert id="insertMallUser" parameterType="com.ruoyi.mall.user.domain.MallUser" useGeneratedKeys="true" keyProperty="id">
        insert into mall_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="nickname != null">nickname,</if>
            <if test="phone != null">phone,</if>
            <if test="avatar != null">avatar,</if>
            <if test="gender != null">gender,</if>
            <if test="city != null">city,</if>
            <if test="openId != null">open_id,</if>
            <if test="status != null">status,</if>
            <if test="totalOrders != null">total_orders,</if>
            <if test="totalAmount != null">total_amount,</if>
            del_flag,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="nickname != null">#{nickname},</if>
            <if test="phone != null">#{phone},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="gender != null">#{gender},</if>
            <if test="city != null">#{city},</if>
            <if test="openId != null">#{openId},</if>
            <if test="status != null">#{status},</if>
            <if test="totalOrders != null">#{totalOrders},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            '0',
        </trim>
    </insert>

    <update id="updateMallUser" parameterType="com.ruoyi.mall.user.domain.MallUser">
        update mall_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="city != null">city = #{city},</if>
            <if test="openId != null">open_id = #{openId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="totalOrders != null">total_orders = #{totalOrders},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="deleteMallUserById" parameterType="Long">
        update mall_user set del_flag = '2' where id = #{id}
    </update>

    <update id="deleteMallUserByIds" parameterType="String">
        update mall_user set del_flag = '2' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
