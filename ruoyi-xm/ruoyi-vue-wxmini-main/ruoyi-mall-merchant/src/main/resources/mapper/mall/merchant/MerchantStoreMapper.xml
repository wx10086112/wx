<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.merchant.mapper.MerchantStoreMapper">

    <resultMap type="com.ruoyi.mall.merchant.domain.MerchantStore" id="MerchantStoreResult">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="name" column="name"/>
        <result property="contact" column="contact"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="longitude" column="longitude"/>
        <result property="latitude" column="latitude"/>
        <result property="businessHours" column="business_hours"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="isMain" column="is_main"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="selectMerchantStoreVo">
        select id, merchant_id, name, contact, phone, address, longitude, latitude,
               business_hours, avatar, status, is_main, create_time, update_time
        from merchant_store
    </sql>

    <select id="selectMerchantStoreList" parameterType="com.ruoyi.mall.merchant.domain.MerchantStore" resultMap="MerchantStoreResult">
        <include refid="selectMerchantStoreVo"/>
        <where>
            and del_flag = '0'
            <if test="merchantId != null">and merchant_id = #{merchantId}</if>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="phone != null and phone != ''">and phone = #{phone}</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by id desc
    </select>

    <select id="selectMerchantStoreById" parameterType="Long" resultMap="MerchantStoreResult">
        <include refid="selectMerchantStoreVo"/>
        where id = #{id} AND del_flag = '0'
    </select>

    <select id="selectMerchantStoreByMerchantId" parameterType="Long" resultMap="MerchantStoreResult">
        <include refid="selectMerchantStoreVo"/>
        where merchant_id = #{merchantId} AND del_flag = '0'
        order by is_main desc, id asc
    </select>

    <insert id="insertMerchantStore" parameterType="com.ruoyi.mall.merchant.domain.MerchantStore" useGeneratedKeys="true" keyProperty="id">
        insert into merchant_store
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="merchantId != null">merchant_id,</if>
            <if test="name != null">name,</if>
            <if test="contact != null">contact,</if>
            <if test="phone != null">phone,</if>
            <if test="address != null">address,</if>
            <if test="longitude != null">longitude,</if>
            <if test="latitude != null">latitude,</if>
            <if test="businessHours != null">business_hours,</if>
            <if test="avatar != null">avatar,</if>
            <if test="status != null">status,</if>
            <if test="isMain != null">is_main,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="merchantId != null">#{merchantId},</if>
            <if test="name != null">#{name},</if>
            <if test="contact != null">#{contact},</if>
            <if test="phone != null">#{phone},</if>
            <if test="address != null">#{address},</if>
            <if test="longitude != null">#{longitude},</if>
            <if test="latitude != null">#{latitude},</if>
            <if test="businessHours != null">#{businessHours},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="status != null">#{status},</if>
            <if test="isMain != null">#{isMain},</if>
        </trim>
    </insert>

    <update id="updateMerchantStore" parameterType="com.ruoyi.mall.merchant.domain.MerchantStore">
        update merchant_store
        <trim prefix="SET" suffixOverrides=",">
            <if test="merchantId != null">merchant_id = #{merchantId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="contact != null">contact = #{contact},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="longitude != null">longitude = #{longitude},</if>
            <if test="latitude != null">latitude = #{latitude},</if>
            <if test="businessHours != null">business_hours = #{businessHours},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isMain != null">is_main = #{isMain},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="deleteMerchantStoreById" parameterType="Long">
        update merchant_store set del_flag = '2' where id = #{id}
    </update>

    <update id="deleteMerchantStoreByIds" parameterType="String">
        update merchant_store set del_flag = '2' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
